---
name: Vault2077
description: 面向超级个体的公共档案仪器
colors:
  archive-paper: "#f3f3ef"
  issued-white: "#fbfbf8"
  registry-carbon: "#11120f"
  body-graphite: "#3f413d"
  index-ash: "#62645e"
  dossier-rule: "#c9cac4"
  dark-divider: "#373a36"
  instrument-black: "#181c1e"
  validation-vermilion: "#bb2e1e"
  pipeline-warning: "#b93822"
  pipeline-ranking: "#0e6f67"
typography:
  display:
    fontFamily: '"Manrope Variable", "Noto Serif SC", "Source Han Serif SC", "Songti SC", STSong, serif'
    fontSize: "clamp(58px, 7vw, 104px)"
    fontWeight: 600
    lineHeight: 1.08
    letterSpacing: "-0.04em"
  headline:
    fontFamily: '"Manrope Variable", "Noto Serif SC", "Source Han Serif SC", "Songti SC", STSong, serif'
    fontSize: "clamp(34px, 3.8vw, 56px)"
    fontWeight: 600
    lineHeight: 1.12
    letterSpacing: "-0.04em"
  title:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Source Han Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
    fontSize: "clamp(19px, 1.65vw, 25px)"
    fontWeight: 600
    lineHeight: 1.45
    letterSpacing: "-0.025em"
  body:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Source Han Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
    fontSize: "15px"
    fontWeight: 400
    lineHeight: 1.72
    letterSpacing: "normal"
  label:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Microsoft YaHei", sans-serif'
    fontSize: "9px"
    fontWeight: 500
    lineHeight: 1.5
    letterSpacing: "0.08em"
  micro:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Microsoft YaHei", sans-serif'
    fontSize: "8px"
    fontWeight: 500
    lineHeight: 1.5
    letterSpacing: "0.06em"
  caption:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Microsoft YaHei", sans-serif'
    fontSize: "10px"
    fontWeight: 400
    lineHeight: 1.55
    letterSpacing: "0.03em"
  note:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Microsoft YaHei", sans-serif'
    fontSize: "11px"
    fontWeight: 400
    lineHeight: 1.55
    letterSpacing: "0.02em"
  small:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Source Han Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1.6
    letterSpacing: "normal"
  navigation:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Source Han Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
    fontSize: "13px"
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: "normal"
  control:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Source Han Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
    fontSize: "14px"
    fontWeight: 500
    lineHeight: 1.45
    letterSpacing: "normal"
  body-large:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Source Han Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
    fontSize: "16px"
    fontWeight: 400
    lineHeight: 1.72
    letterSpacing: "normal"
  reading:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Source Han Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
    fontSize: "17px"
    fontWeight: 400
    lineHeight: 1.9
    letterSpacing: "normal"
  lede:
    fontFamily: '"Manrope Variable", "Noto Sans SC", "Source Han Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
    fontSize: "18px"
    fontWeight: 400
    lineHeight: 1.7
    letterSpacing: "normal"
  service-title:
    fontFamily: '"Manrope Variable", "Noto Serif SC", "Source Han Serif SC", "Songti SC", STSong, serif'
    fontSize: "clamp(22px, 7cqw, 68px)"
    fontWeight: 600
    lineHeight: 1.08
    letterSpacing: "-0.04em"
  home-brand:
    fontFamily: '"Manrope Variable", "Noto Sans SC", sans-serif'
    fontSize: "clamp(84px, 10.4vw, 160px)"
    fontWeight: 650
    lineHeight: 0.88
    letterSpacing: "-0.04em"
  sic-channel-mark:
    fontFamily: '"Manrope Variable", "Noto Sans SC", sans-serif'
    fontSize: "clamp(88px, 10vw, 152px)"
    fontWeight: 700
    lineHeight: 0.8
    letterSpacing: "-0.04em"
  surface-20:
    fontSize: "20px"
  surface-21:
    fontSize: "21px"
  surface-23:
    fontSize: "23px"
  surface-24:
    fontSize: "24px"
  surface-26:
    fontSize: "26px"
  surface-27:
    fontSize: "27px"
  surface-28:
    fontSize: "28px"
  surface-29:
    fontSize: "29px"
  surface-30:
    fontSize: "30px"
  surface-31:
    fontSize: "31px"
  surface-32:
    fontSize: "32px"
  surface-35:
    fontSize: "35px"
  surface-36:
    fontSize: "36px"
  surface-37:
    fontSize: "37px"
  surface-38:
    fontSize: "38px"
  surface-39:
    fontSize: "39px"
  surface-40:
    fontSize: "40px"
  surface-42:
    fontSize: "42px"
  surface-43:
    fontSize: "43px"
  surface-44:
    fontSize: "44px"
  surface-46:
    fontSize: "46px"
  surface-48:
    fontSize: "48px"
  surface-50:
    fontSize: "50px"
  surface-52:
    fontSize: "52px"
  surface-53:
    fontSize: "53px"
  surface-54:
    fontSize: "54px"
  surface-60:
    fontSize: "60px"
  surface-62:
    fontSize: "62px"
  surface-64:
    fontSize: "64px"
  surface-66:
    fontSize: "66px"
  surface-70:
    fontSize: "70px"
  surface-72:
    fontSize: "72px"
  surface-74:
    fontSize: "74px"
  surface-76:
    fontSize: "76px"
  surface-78:
    fontSize: "78px"
  surface-82:
    fontSize: "82px"
  surface-86:
    fontSize: "86px"
  surface-88:
    fontSize: "88px"
  surface-92:
    fontSize: "92px"
  surface-100:
    fontSize: "100px"
  surface-102:
    fontSize: "102px"
  surface-108:
    fontSize: "108px"
  surface-110:
    fontSize: "110px"
  surface-112:
    fontSize: "112px"
  surface-116:
    fontSize: "116px"
  surface-126:
    fontSize: "126px"
  surface-132:
    fontSize: "132px"
  surface-174:
    fontSize: "174px"
  surface-264:
    fontSize: "264px"
rounded:
  none: "0"
spacing:
  pane-gap: "12px"
  control-inline: "18px"
  gutter: "clamp(22px, 4vw, 72px)"
  section: "clamp(104px, 11vw, 176px)"
components:
  button-primary:
    backgroundColor: "{colors.registry-carbon}"
    textColor: "{colors.issued-white}"
    typography: "{typography.label}"
    rounded: "{rounded.none}"
    padding: "10px 17px"
    height: "46px"
  button-primary-hover:
    backgroundColor: "{colors.archive-paper}"
    textColor: "{colors.registry-carbon}"
    typography: "{typography.label}"
    rounded: "{rounded.none}"
    padding: "10px 17px"
    height: "46px"
  filter-default:
    backgroundColor: "transparent"
    textColor: "{colors.registry-carbon}"
    typography: "{typography.label}"
    rounded: "{rounded.none}"
    padding: "12px 18px"
    height: "44px"
  filter-active:
    backgroundColor: "{colors.registry-carbon}"
    textColor: "{colors.issued-white}"
    typography: "{typography.label}"
    rounded: "{rounded.none}"
    padding: "12px 18px"
    height: "44px"
  field:
    backgroundColor: "transparent"
    textColor: "{colors.registry-carbon}"
    typography: "{typography.body}"
    rounded: "{rounded.none}"
    padding: "13px 14px"
  navigation-active:
    backgroundColor: "{colors.issued-white}"
    textColor: "{colors.registry-carbon}"
    typography: "{typography.body}"
    rounded: "{rounded.none}"
    padding: "10px 18px"
    height: "76px"
  registry-row:
    backgroundColor: "transparent"
    textColor: "{colors.registry-carbon}"
    typography: "{typography.body}"
    rounded: "{rounded.none}"
    padding: "30px 0"
  registry-row-focus:
    backgroundColor: "{colors.registry-carbon}"
    textColor: "{colors.issued-white}"
    typography: "{typography.body}"
    rounded: "{rounded.none}"
    padding: "30px 0"
---

# Design System: Vault2077

## Overview

**Creative North Star: "公共档案仪器"**

Vault2077 的界面应像一件面向公众开放的档案仪器：信息可以被阅读，也可以被定位、核验和操作。系统以档案纸、登记碳黑、细线、编号、栏位和明确反色组织复杂内容，让高密度信息保持克制、精确和制度化。

未来探索感来自尺度反差、连续栏线、全栏状态变化和少量受控运动，而不是霓虹、发光或科幻装饰。中文衬线标题赋予公共记录的正式感；无衬线正文与标签维持当代工具的清晰度。所有表达都应服务于事实层级、责任边界和操作状态。

**Key Characteristics:**

- 纸张色阶与碳黑反色构成主要视觉材料。
- 中文衬线大标题与无衬线正文形成明确角色分工。
- 非对称网格、连续细线和编号建立档案秩序。
- 零圆角、无环境阴影，组件保持方正和可扫描。
- 默认状态安静，交互状态通过反色、下划线或短距离位移明确出现。

## Colors

颜色系统接近印刷档案：暖灰纸面承载阅读，黑与深灰承担结构和状态，朱红只用于必须处理的错误。

### Primary

- **登记碳黑** (#11120f): 用于主要文字、关键分隔线、选中状态和高优先级操作；整栏反色时与签发白成对使用。

### Neutral

- **档案纸** (#f3f3ef): 全站默认页面底色，是最主要的阅读表面。
- **签发白** (#fbfbf8): 用于轻微提亮的活动表面、反色文字和少量高优先级区块。
- **正文石墨** (#3f413d): 用于正文、说明和次于标题的长文本。
- **索引灰** (#62645e): 用于编号、元数据、时间、状态补充和辅助说明；在档案纸与浅灰档案表面上都满足普通文字 WCAG AA 对比度。
- **卷宗线** (#c9cac4): 用于默认分隔线、表格线和非活动边界。
- **暗页分隔** (#373a36): 用于深色表面上的结构线。
- **仪器黑** (#181c1e): 用于需要与正文页面区分的整段深色频道表面。
- **校验朱红** (#bb2e1e): 只用于错误、危险操作和阻断性校验，不承担品牌装饰。
- **管线警示** (#b93822): 只用于信息管线中的阻断、欠账和异常状态；与文字标签或结构位置共同表达含义。
- **管线榜单** (#0e6f67): 只用于信息管线中的确定性榜单通道；在管线纸面及反色状态上保持 AA 对比度。

### Named Rules

**The Evidence Before Decoration Rule.** 颜色必须表达层级、状态、责任或频道边界；不为填满画面而增加色彩。

**The Rare Reversal Rule.** 大面积碳黑反色只用于明确选中、聚焦或章节转换；唯一的静态身份例外是首页 `2077` 品牌签发牌。它不可交互、不可复用于普通标题或状态，反色的稀缺性仍然构成强调效果。

## Typography

**Display Font:** Manrope Variable 配合 Noto Serif SC、Source Han Serif SC、宋体系统回退  
**Body Font:** Manrope Variable 配合 Noto Sans SC、Source Han Sans SC 与系统无衬线回退  
**Label/Mono Font:** Manrope Variable 配合中文无衬线回退，启用等宽数字

**Character:** 中文标题借助衬线回退形成正式、公共和可归档的声音；拉丁字母、数字、正文和控件保持工具化精度。字号反差可以显著，但字重和颜色数量必须克制。

### Hierarchy

- **Display** (600, fluid 58–104px, line-height 1.08, letter-spacing -0.04em): 用于频道首屏和一级页面标题；允许形成强烈尺度，但不得造成横向溢出。
- **Headline** (600, fluid 34–56px, line-height 1.12, letter-spacing -0.04em): 用于主页区块与主要章节标题。
- **Title** (600, fluid 19–25px, line-height 1.45, letter-spacing -0.025em): 用于记录、列表和内容模块标题。
- **Body** (400, 15px, line-height 1.72): 用于说明与持续阅读；正文通常限制在约 31–45 个中文字符或 65–75 个拉丁字符的舒适行宽。
- **Label** (500, 9px, line-height 1.5, letter-spacing 0.08em): 用于频道代码、编号、时间、字段名和元数据；数字采用 tabular forms。
- **Dense Operational Steps** (8px, 10px, 11px, 12px, 13px, 14px): 用于高密度目录中的编号、注释、补充说明、导航和控件；小字号必须使用满足 4.5:1 的索引灰或更深颜色。
- **Reading Steps** (16px, 17px, 18px): 用于事实带、列表、长段说明与导语，按阅读宽度逐级增加行高。
- **Service Title** (600, fluid 22–68px, line-height 1.08, letter-spacing -0.04em): 用于 OPC 右侧服务名称；所有宽度保持单行，并由阅读栏容器宽度决定字号。
- **Home Brand** (650, fluid 84–160px, line-height 0.88, letter-spacing -0.04em): 只用于首页 `Vault2077` 签发标题。
- **SiC Channel Mark** (700, fluid 88–152px, line-height 0.8, letter-spacing -0.04em): 只用于 SiC 频道首屏标识。
- **Implemented Surface Ramp** (20–112px): 记录现有频道首屏、榜单、声明和服务页已经投入使用的展示字号端点。新界面优先使用 Display、Headline、Title 等语义角色；只有在延续同类既有表面时才复用对应 surface step。

### Named Rules

**The Chinese Carries Ceremony Rule.** 正式感由中文衬线标题承担；正文、导航和数据标签不模仿标题的装饰性。

**The Scale Has a Job Rule.** 超大字号只用于定义频道、页面或具体服务，不用于普通卡片和重复列表。

## Layout

全站内容容器上限为 1480px，水平边距使用流体 gutter，主要章节采用更大的流体纵向节奏。桌面端以非对称网格表达主次：常见结构包括宽主栏与窄辅助栏、编号脊柱与正文、或多栏服务目录。12px 的窄缝只用于主页瀑布等明确的并列表面；主要阅读结构依靠留白和连续细线分区。

标准验收宽度为 360、768、1280 和 1440px。约 1100px 以下开始压缩复杂目录与记录网格，约 820px 以下进入移动卷宗流：工作台改为纵向结构，主导航切换为覆盖式频道索引，多栏记录重排为单列。移动端保留信息、操作和状态，不把桌面结构等比例缩小。

移动卷宗流只在频道切换、章节切换和状态切换处保留强分隔线；普通记录、元数据与同组内容依靠留白、字号、缩进和纸张色阶建立层级。首页四个一级频道属于核心导航，窄屏必须一次完整呈现，不能依赖用户发现横向滚动；频道首屏的辅助快捷入口可以采用可横向滚动、可触摸的直角任务轨，但频道正式目录仍须服从对应专项规格的结构与反色边界；页脚目录压缩为双列。移动功能文字不小于 11px，正文保持至少 16px，主要交互目标不小于 44px，并为安全区保留空间。

共享移动样式层只拥有断点 token、安全区、站点头部、频道首屏骨架、任务轨和页脚；Home、Feed、SiC、Frontier 的内部重排分别保存在对应路由移动样式中，不能把频道内部选择器重新集中到共享末层。覆盖式频道索引打开时必须锁定背景滚动、使正文与页脚不可交互、把焦点移入索引并循环约束在菜单控件内，同时支持 `Escape` 关闭并把焦点返回菜单按钮；视口离开移动断点时必须立即关闭索引并恢复原有 `inert` 与滚动状态，不能让已隐藏的移动控件继续持有页面锁。

连续栏线具有结构含义：同一工作区的分隔线应贯穿所属区域，选中状态应铺满栏位或延伸至视口边缘，不能成为四周留缝的悬浮色块。这里的“页面边缘”始终指浏览器视口的物理边缘（`0` 与 `100vw`），绝不是 `.shell`、内容容器或栅格内留白的外沿。

### Named Rules

**The Connected Register Rule.** 细线必须连接真实的信息结构；不要在相邻区域随意中断或重复描边。

**The Responsive Recomposition Rule.** 窄屏通过重排与删减重复元数据保持任务完整，而不是单纯缩小字号。

**The Mobile Dossier Stream Rule.** 移动端把内容组织成连续卷宗流；强线只标记频道、章节或状态变化，普通层级不重复描边。不得把桌面栏线逐条折叠后继续堆叠在窄屏。

**The Viewport-True Reversal Boundary Rule.** 行级、目录级和频道级的反色状态必须由真实的按钮、链接或可聚焦记录行自身承载：反色背景、点击命中区和键盘焦点区共享同一个盒模型边界。栏目处于当前布局平面的最左侧时，该元素必须从栏位内侧的真实分割线延展到视口左缘；处于最右侧时，必须从真实分割线延展到视口右缘；中间栏位只在两侧真实分割线之间延展。多栏在窄屏折叠为单栏后，整行反色必须同时抵达两侧视口边缘。文字仍以元素自身的内边距对齐内容栅格。`.shell`、内容容器和 gutter 都不是页面边缘。不得借助超出命中区域的伪元素、`100vmax` 阴影、裁剪溢出或覆盖层制造更大的反色面；应扩展控件本身的宽度、外边距和内边距，且不得越过不属于该栏目结构的分割线。具有自身完整边框的紧凑操作按钮不强制延展。

**The Optical Center Rule.** 对具有明确最小高度的紧凑行按钮、目录项和导航项，编号、主标签、辅助标签与图标组成的内容组必须以视觉中心落在控件几何中心附近；不得用仅为基线补偿而存在的顶部内边距把整组内容推向上方。只有高度由多行内容自然增长的阅读型记录，才可以采用顶部对齐。

## Elevation & Depth

系统全局保持平面化，不使用环境阴影模拟悬浮卡片。深度来自纸张色阶、连续细线、留白、整栏反色与前后层级的尺度差。粘性导航可以使用轻微背景模糊保持上下文，但不得产生玻璃拟态表面。

`box-shadow` 只用于焦点内描边等局部状态，不得用于伪造跨栏或跨视口的反色范围。需要整栏或贴边反色时，应让真实的控件或其直接结构本身延展到对应分割线；当该栏位是外侧栏时，延展必须到视口物理边缘，而不是停止在 shell 的 gutter。

### Named Rules

**The Flat-by-Default Rule.** 静止表面没有投影；状态变化优先使用反色、线条和位移。

## Shapes

主要组件使用零圆角。按钮、输入框、筛选器、表格、导航单元和内容区域都以直角、细边框和清晰栏线构成。圆形只保留给真实需要圆形语义的少量图形或人物表现，不扩展为通用组件语言。

边框通常为 1px；焦点可使用 2px 外描边或内描边。形状的职责是显示范围与对齐，不是制造柔和或亲切感。

### Named Rules

**The Square Instrument Rule.** 工具与记录组件保持方正；不要用圆角弱化责任边界。

## Components

组件像登记表与精密仪器面板：默认安静、可扫描，状态反馈直接且短促。

### Buttons

- **Shape:** 直角矩形，最小高度约 46px。
- **Primary:** 登记碳黑底、签发白文字，紧凑水平内边距；深色区域可反向使用签发白底与仪器黑文字。
- **Hover / Focus:** 主按钮悬停切换为纸面或透明底；键盘焦点使用清晰 2px 描边，不依赖颜色变化。
- **Secondary / Ghost:** 透明底与细边框，活动状态切换为碳黑反色。
- **Vertical Alignment:** 具有固定或最小行高的目录按钮将完整文字组和状态标记垂直居中；多行内容驱动高度的阅读型记录保持顶部对齐。

### Chips

- **Structural Reversal:** 行级和目录级反色由真实按钮、链接或可聚焦记录行直接填充到所属结构边界。外侧栏的控件盒子跨越 shell gutter 并贴住视口边缘；内部栏停在相邻分割线。文字内缩通过该元素自身的 padding 保持，不另设超出命中区域的背景层。

- **Style:** 筛选项使用相邻直角单元，而不是胶囊；默认透明底、卷宗线边框。
- **State:** 活动项使用登记碳黑与签发白，且必须保留清楚的键盘焦点。

### Cards / Containers

- **Corner Style:** 零圆角。
- **Background:** 默认沿用档案纸；主页并列频道可使用非常接近纸张的轻微色阶区分。
- **Shadow Strategy:** 不使用环境阴影。
- **Border:** 用顶部线、底部线或连续栏线定义结构，避免每个内容块四边封闭。
- **Internal Padding:** 使用流体 gutter 或 18–34px 的局部间距；同组组件保持共同基线。

### Inputs / Fields

- **Style:** 透明底、1px 登记碳黑边框、零圆角，约 13px × 14px 内边距。
- **Focus:** 2px 登记碳黑描边并与控件保持 2px 间隔。
- **Error / Disabled:** 错误使用校验朱红左侧标尺与文字；禁用状态降低不透明度并使用不可用光标。

### Navigation

桌面导航由相邻的方形频道单元组成，使用细竖线分隔；每项含小号英文代码和中文名称。活动项轻微提亮，并在底部出现 3px 状态线。移动端切换为覆盖可视区域的两列频道索引，以纸张色阶和整块反色表达默认与当前频道，不复刻桌面栏线。

### Homepage Brand Issuance

首页一级标题是一个语义完整的 `Vault2077`，视觉上由纸面碳黑 `Vault` 与碳黑底、签发白字的 `2077` 组成。`2077` 两侧必须保留明确的光学边距，不得贴边、裁字或呈现按钮、选中项和遮盖状态的交互暗示。

每次完整加载或刷新首页时允许执行一次品牌签发序列：字符以可辨认的节奏按登记顺序出现，`2077` 只进行一次短促校验闪烁，随后碳黑签发面从左向右完成反色。完整序列约 2.3 秒，不阻断导航和正文；同一页面实例内不重复播放，页面隐藏或组件卸载时立即稳定为最终状态。动效只使用局部透明度、裁切、颜色与合成变换，不移动布局，也不引入通用动效依赖。

服务端与脚本失败的默认状态始终是完整静态标题；辅助技术始终只接收一次完整 `Vault2077` 名称，不逐字播报。`prefers-reduced-motion` 直接显示最终签发状态，不播放登记或闪烁。

### Registry Rows

记录行以编号、类别、主标题、摘要和数据栏构成。默认只显示线性结构；焦点或关键选中状态可让碳黑背景延展至整个栏位，并使标题产生约 4–7px 的短距离位移。整行状态必须保持文字对比度与元数据可读性。

### OPC Service Browser

OPC 工作台使用一级入口、目录与阅读区组成的连续多栏登记结构。第一栏反色状态延伸到视口左缘，内部选中项铺满相邻栏线；阅读区通过元数据、单行服务标题、价格周期带和带编号的内容模块建立层级。窄屏按相同信息顺序纵向重组。

中栏分类行的按钮、悬停和选中表面始终铺满两侧真实分栏线，分类名称、公开说明和右侧数量则使用与中栏标题、具体服务记录相同的水平 gutter，不贴住栏线。分类说明是目录中受控发布的正式内容：基础设施分组和专项领域说明概括所覆盖的经营能力，游骑兵身份说明定义整类顾问的专业支持性质；不得用项目数量生成说明，也不得从某位顾问的标签、介绍或专业方向派生。

### OPC Ranger Dossier Shelf

游骑兵协会右侧先以已经有顾问入驻的常态文案建立合作语境，再接一座姓名书架；它不是头像墙或索引。文案区以一条横线结束，书脊从该横线开始并自动伸展至右栏下边框，上下不得留白。所有书脊在任何宽度下都保持竖向，只写居中放置的公开姓名；空席只写“待公开”，绝不以顾问身份冒充姓名。默认所有书脊收拢，鼠标悬停或键盘聚焦时当前书脊反色并以约 1.25 秒的舒展节奏在原位拉开，大头像以更缓的动势完成；几何变化先完成布局，再以 FLIP 合成变换平滑过渡，避免逐帧动画化 `flex-grow`、网格列或宽度。鼠标离开整座书架即以较短节奏收回，`prefers-reduced-motion` 直接切换最终布局。展开面由填满区域的大头像构成，只允许叠加主要顾问身份、公开姓名与可选的个人签名，不出现简介、标签、编号、模板说明或独立“查看详情”按钮；真实顾问的整幅展开头像是指向稳定档案路由的链接，书脊本身只负责自动展开。窄屏未展开的真实顾问书脊仍保留至少 30px 可见宽度。个人签名是顾问本人的一句话表达，最长 120 个字符，置于姓名下方并最多显示两行；未填写时不显示也不生成占位。身份、姓名与签名直接署在头像上，不使用实心或半透明矩形底板；复杂和彩色头像只通过融入整幅画面的无边界底部渐隐与轻微文字阴影维持对比，不形成独立卡片。书架按固定容量分页；宽屏将无边框、无底色的翻页三角固定在书架右侧页边留白，下一页朝右、返回朝左，不得覆盖书脊、展开头像或移入左侧导航；未来中间页同时需要前后翻页时，两枚三角只在右侧上下错开。窄屏留白不足时改为贴边的 2px 纯黑竖条。书架承担视觉认识而非主导航，文案从用户问题与合作关系出发，不复述鼠标操作、准备状态、版式或数据规则。

### OPC Ranger Public Dossier

游骑兵个人档案延续全站档案纸、碳黑脊柱、连续细线、衬线主标题与无衬线登记字段。首屏先以并列的姓名区和公开肖像识别人；姓名区只放登记信息、公开姓名和一句话介绍。个人签名属于顾问本人的声音，不与姓名争夺标题层级：如有签名，在主画面下方设置一条横贯姓名与肖像两栏的自述签发带，再接核验、更新与身份登记栏。签发带只用横线、栏线、留白和字体层级，不用卡片、色块或引号装饰。宽屏将 `STATEMENT / 个人签名` 与原文分栏，并让签发带和核验登记栏等高，所有栏内信息垂直居中；窄屏改为上下排列并恢复内容驱动高度。120 个字符完整自然换行，连续字符强制断行，页面可以增高但不得侵入头像或产生横向滚动。未填写签名时整条签发带消失，核验栏直接承接主画面。

## Do's and Don'ts

### Do:

- **Do** 让行级反色的视觉范围、点击范围与键盘焦点范围严格重合；外侧栏必须贴住视口物理边缘，内部栏必须严格停在真实分割线。
- **Do** 使用纸张色阶、细线、编号和留白表达真实的信息层级。
- **Do** 让选中、焦点与活动状态覆盖完整结构范围，并维持清楚对比度。
- **Do** 保持中文标题、正文、数据标签各自明确的字体角色。
- **Do** 在移动端重组网格，保留主要阅读、操作和状态。
- **Do** 让运动服从操作反馈，并完整遵循 `prefers-reduced-motion`；减弱动效时保留即时反色、边框和状态文案，不使用全局极短时长清除所有反馈。

### Don't:

- **Don't** 把 `.shell` 的 gutter、内容容器外沿或栅格留白误当作页面边缘；也不要用 `100vmax` 阴影、脱离控件的伪元素或裁剪溢出，让反色背景先于控件本身出现或跨越无关栏目。
- **Don't** 把界面改造成圆角卡片式 SaaS 仪表盘。
- **Don't** 使用霓虹、发光、赛博朋克色彩或装饰性未来主义。
- **Don't** 使用柔软生活方式品牌的胶囊、柔和阴影和情绪化留白。
- **Don't** 依靠环境阴影制造层级，或让内容块漂浮在纸面上。
- **Don't** 为普通列表滥用超大标题、整栏反色或复杂动效。
- **Don't** 用颜色作为唯一状态信号，或在真实长文本下制造横向溢出。
