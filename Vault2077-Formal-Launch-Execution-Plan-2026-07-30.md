# Vault2077 正式上线执行计划书

制定日期：2026-07-30
更新说明：2026-07-31 起，具体基础设施规格、RDS 计费和免费管理员身份方案以 `Vault2077-Production-Deployment-Plan-2026-07-31.md` 为最新执行基线；本文其余功能门禁继续有效。
范围确认更新：2026-07-30（OPC 服务与游骑兵采用后台渐进发布，不设首发数量下限）
计划目标：以最短关键路径关闭所有 P0，上线一个可以真实阅读、真实下单、真实核验、真实运维和可安全回滚的 Vault2077 V1
依据：`Vault2077-Launch-Readiness-Audit-2026-07-30.md`

## 1. 执行结论

当前最重要的决定不是继续增加功能，而是进入 **发布冻结期**：

1. 不再新增频道、服务分类、榜单、支付方式或后台模块。
2. 只处理阻止上线的问题、真实业务输入、生产部署和验收证据。
3. 所有工作围绕一条关键路径推进：

> 业务规则冻结 → 外部账号与备案 → 生产数据层 → 支付/身份/调度配置 → 真实内容基线 → 全链路演练 → 浏览器与安全验收 → 发布签字

在所有外部资源可立即获得的情况下，目标周期为 **13 个有效工作日**。ICP备案、支付签约、域名或云资源审批可能成为更长的外部关键路径，必须在第 0 天启动。

## 2. 正式上线范围

### 2.1 本次必须上线

- 首页和四个一级频道；
- Vault 事件簿、资讯瀑布、路边社及详情；
- 匿名纠错提交与后台处理；
- OPC 服务目录的后台草稿、校验、发布和公开读取；首发只发布已确认可履约的真实服务，不设条目数量下限；
- OPC 服务目录正式发布；在线订单与支付宝付款按最新确认延期，首发保持显式关闭；
- 游骑兵后台维护、授权校验和诚实空状态；首发不设专家人数下限；
- SiC 论文、档案、课程、播客与 5 个平台原生榜；
- 边境计划报名、挑战验证、排行榜、奖品捐献和自动结算；
- 受原生 WebAuthn/Passkey 和可撤销会话保护的运营后台；
- 统一采集、境内 Worker、Frontier Tick、健康检查和告警；
- 隐私、条款、ICP备案信息、数据保留和责任边界。

### 2.2 本次不增加

- 普通用户账户；
- 购物车；
- 新支付方式；
- 新 OPC 一级分类；
- 新榜单来源；
- 评论、社区、消息系统；
- Redis 或对象存储，除非压力测试证明当前架构不能达标；
- 非 P0 的视觉重构。

### 2.3 发布降级原则

如果某个业务输入无法按期完成，只允许以下两种处理：

- 整个能力在生产环境显式关闭，并从主要行动入口撤下；
- 延后正式上线。

不允许使用演示档案、虚假内容、测试订单、待公布奖励或含糊文案伪装功能已经可用。

## 3. 组织方式

即使由一人承担，也应按角色分别签字，避免同一判断未经复核直接上线。

| 角色 | 主要责任 | 必须签字的内容 |
| --- | --- | --- |
| 发布负责人 | 排期、阻塞升级、版本冻结、Go/No-Go | 最终发布结论 |
| 产品/运营 | OPC 内容、顾问档案、赛季、奖励、内容基线 | 真实业务输入 |
| 工程 | 代码修复、迁移、支付、采集、后台 | 构建、测试、E2E |
| 基础设施/SRE | 域名、RDS、Nginx、systemd、监控、回滚 | 生产运行证据 |
| 安全/隐私 | Passkey、密钥、数据边界、日志、恢复 | 安全与隐私验收 |
| 法律复核 | 条款、隐私、退款、顾问、奖品、版权 | 法律文本确认 |

每天只开一次 15 分钟发布检查：

- 昨日完成的证据；
- 今日关键路径；
- 新增阻塞和负责人；
- 是否需要改变发布日期。

## 4. 六条并行工作流

## 工作流 A：业务规则与合规

负责人：产品/运营 + 法律复核
目标完成日：第 3 天；ICP备案按外部流程单独跟踪

### A1. 冻结 Frontier 规则

- 确定当前赛季是否为真实公开赛季；
- 确定官方冠军奖励的真实名称、价值、交付主体和交付条件；
- 确定随机奖品分配、撤回、顺延和争议处理；
- 明确只有冠军永久退出，还是采用其他规则；
- 同步更新规范、页面、规则弹窗和 `/terms`；
- 增加生产发布门禁：奖励或条款未确认时不得开放报名和捐献。

验收证据：

- 页面、条款、规范、测试使用同一规则；
- 代码中不再出现“待公布”；
- 自动化测试覆盖规则一致性；
- 产品与法律签字。

### A2. 建立 OPC 渐进发布纪律

- 首发不要求填满 7+14 项；逐项审核准备公开的服务名称、范围、排除项、材料、成果、验收、周期和价格；
- 明确第三方费用、取消、退款、变更与争议处理；
- 通过后台发布第一个正式修订，保留修订号和生效时间；
- 确认真实服务交付联系人和订单处理流程。

验收证据：

- 所有已公开服务逐项通过发布校验且能够真实履约；
- 数据库中存在带时间的正式发布快照；
- 任意订单能追溯到服务修订和价格快照；
- 未完成服务保留为草稿，绝不以示例内容公开；
- 产品、运营与法律签字。

### A3. 游骑兵渐进发布

- 首发允许 0 份档案，人数不构成上线阻塞；
- 后台逐份增加本人授权、职业信息可核验、联系方式可公开的档案；
- 每份公开档案包含核验日期、资料更新时间和撤销联系人；
- 0 份时显示诚实空状态，不使用占位头像、虚构履历或“即将加入”冒充真实目录；
- 每次发布后逐个测试公开详情和邮件联系入口。

### A4. 法律与备案

- 修正 Frontier 旧条款；
- 复核隐私说明中的数据控制主体、联系渠道、数据用途、保留、删除和用户权利；
- 复核 OPC 退款、消费者权利和独立顾问责任；
- 复核内容摘要、版权、纠错和来源链接规则；
- 立即提交 ICP 备案；
- 取得备案号后在首页底部展示并链接工信部查询系统。

验收证据：

- 所有公开法律文本有版本和生效日期；
- 页脚不再出现“待接入”；
- 备案号可以在官方系统查询；
- 法律复核签字。

## 工作流 B：生产基础设施与数据

负责人：基础设施/SRE + 工程
目标完成日：第 5 天

### B1. 建立生产环境

- 确定公开域名和独立后台域名；
- 建立应用服务器和同 VPC RDS PostgreSQL 17；
- Node 只监听回环地址；
- 安全组只开放 80/443 和必要运维入口；
- RDS 只接受应用安全组访问；
- 开启 TLS、删除保护、自动备份、日志备份、PITR 和监控。

### B2. 执行迁移

- 在空数据库执行 `npm run db:migrate`；
- 再次执行验证幂等；
- 运行 PostgreSQL 集成测试；
- 初始化 OPC 正式目录；
- 确认生产环境禁用文件预览存储。

验收证据：

- 5 个迁移全部记录成功；
- 二次迁移无副作用；
- `state_documents`、inbox、audit、rate_limit、admin_sessions 表存在；
- `npm run test:postgres:integration` 通过。

### B3. 完成备份恢复

- 生成一组脱敏测试业务数据；
- 执行真实自动或手工备份；
- 恢复到隔离 RDS；
- 对比表数量、关键记录和审计链；
- 记录 RPO、RTO、负责人、日期和异常。

上线标准：

- 恢复后的关键数据完整；
- RPO/RTO 达到团队明确接受的目标；
- 恢复不是只看控制台“成功”，而是实际读取验证。

## 工作流 C：安全、身份与生产配置

负责人：安全/隐私 + 工程
目标完成日：第 5 天

### C1. 生成和托管生产密钥

分别生成，不得复用：

- 管理会话密钥；
- 审计哈希密钥；
- 采集 Worker 密钥；
- Frontier 公开任务密钥；
- Frontier Tick 密钥；
- 健康检查密钥；
- 敏感数据密钥环；
- 管线签名密钥环。

要求：

- 每个密钥有 owner、用途、创建日期、活动 key ID 和轮换日期；
- 不进入 Git、日志、构建产物或浏览器；
- 演练新旧密钥重叠、切换和撤旧。

### C2. 配置原生 Passkey

- 生产后台使用 `admin.superones.top` 独立 HTTPS 域名，并从该主机名派生 WebAuthn RP ID；
- 唯一生产管理员固定为 `lanzhouda@163.com`，服务器只保存凭证公钥、ID、计数器和审计信息；
- 通过 SSH 生成十分钟一次性注册令牌，在真实设备完成首次注册并离线保存首次显示的恢复码；
- 验证 challenge 单次消费、用户验证、来源/RP ID、计数器冲突和五分钟再认证；
- 验证凭证撤销、单会话撤销、恢复码单次消费和 SSH 全量恢复；
- 验证公开域名、服务器 IP 和 Node 端口无法进入后台。

Cloudflare 和外部 IDaaS 都不进入当前生产身份链路。剩余工作是在目标 TLS 域名和真实认证器上完成端到端验收。

### C3. 关闭全部生产配置错误

目标命令：

```powershell
$env:NODE_ENV = "production"
npm.cmd run deploy:check
```

上线标准：

- 0 error；
- warning 必须逐条记录接受理由；
- 输出中不包含密钥或私人信息。

## 工作流 D：支付关闭验收与后续接入

负责人：工程 + 运营
目标完成日：首发关闭态第 3 天；真实接入另行排期

### D1. 首发关闭态

- 设置 `VAULT2077_OPC_PAYMENTS_ENABLED=false`；
- 确认公开页面不渲染订单表单、不收集联系人；
- 确认订单 API 返回 503，服务目录仍可浏览；
- 确认 `deploy:check` 在支付宝变量为空时仍可通过。

### D2. 后续支付宝生产准备

- 应用上线；
- 开通电脑网站支付和手机网站支付；
- 配置 APPID、商户 PID、应用私钥、支付宝公钥、RSA2、正式网关；
- 配置公开通知地址和返回地址；
- 确认服务器时间同步。

### D3. 真实小额演练

使用受控真实服务和最小金额，按顺序完成：

1. 创建订单；
2. 验证服务修订和服务器价格快照；
3. PC 收银台跳转；
4. 手机收银台跳转；
5. 完成付款；
6. 验证异步通知自动更新已到账；
7. 重放相同通知；
8. 构造金额不一致并确认拒绝；
9. 模拟通知缺失并执行主动查询；
10. 标记交付完成；
11. 执行退款并登记；
12. 验证审计记录和联系方式加密。

上线标准：

- 每一步都有订单号、时间和结果；
- 浏览器返回页面从不直接显示“已到账”；
- 重复提交不创建重复订单；
- 运营人员能按订单号完成异常对账。

## 工作流 E：真实内容与调度

负责人：产品/运营 + 工程
目标完成日：第 8 天

### E1. 维持来源门禁

- 已处理 3 份新来源审计文档的元数据、状态和索引；
- 已将 SiC 来源目录同步为 22 approved / 11 retired / 1 pending_review；
- 每次来源变更继续同步来源目录、注册表、bundle 和说明；
- `npm run docs:check` 必须持续通过。

### E2. 配置双编辑通道

- 补齐 Vault 与 SiC 主提供方；
- 按需配置一个受控备用；
- 配置独立并发、批大小、预算和超时；
- 演练主提供方失败、备用切换、预算耗尽和长积压；
- 验证 rankings 和 Frontier 不经过编辑模型。

### E3. 生成真实上线基线

Vault 最低验收目标：

- 使用最近 30 天真实来源；
- 资讯瀑布和路边社不为空；
- 至少形成 1 个由多个发布方和不同角色支持的可验证事件；
- 每条内容可回到原始来源；
- 没有演示数据、陈旧填充或无依据摘要。

SiC 最低验收目标：

- 每个 approved 来源至少 1 条符合边界的最新正式内容；
- 论文、档案、课程和播客页面状态明确；
- 5 个榜单均有成功快照或明确 stale 状态；
- 修正公开术语中 `documents` 与“档案”的不一致。

### E4. 安装调度

- GitHub Actions 四 lane schedule 启用；
- 境内安装 Acquisition Worker timer；
- 境内安装 Frontier Tick timer；
- 验证漏跑补跑、失败重试、quarantine 和恢复；
- 验证 schedule 不是唯一业务时钟。

## 工作流 F：质量、监控与发布

负责人：工程 + SRE + 发布负责人
目标完成日：第 12 天

### F1. 补齐功能缺口

- 为纠错报告增加实际内容修正动作，至少支持隐藏错误记录和重新编排；
- 明确跨存储半提交的补偿/重放机制；
- 保持 Frontier 奖励和条款一致性；
- 已增加“生产未确认奖励时禁止报名/验证/捐献”的配置门禁与测试；
- 已增加页脚 ICP 配置和生产检查。

### F2. 监控与告警

必须覆盖：

- Web 5xx 和进程退出；
- PostgreSQL 连接、容量和慢查询；
- Inbox 积压、最老任务延迟、retryable、quarantine；
- 四通道内容新鲜度；
- Vault/SiC 模型错误、预算、备用切换；
- 五个榜单 stale；
- Frontier Tick 失败和公开任务积压；
- 支付异步通知失败和待付款超时；
- 备份失败；
- TLS/域名证书到期。

每个告警必须有：

- 阈值；
- 接收人；
- 处理步骤；
- 测试告警记录；
- 恢复确认。

### F3. 浏览器验收

在 360 / 768 / 1280 / 1440 四个宽度逐项完成：

- 首页与主导航；
- Vault 三栏、详情、加载与纠错；
- OPC 三入口、筛选、详情、订单、付款返回；
- SiC 内容展开、榜单展开/翻面/复制；
- Frontier 规则弹窗、报名、验证、捐献、排行榜；
- 后台登录、再认证、目录、订单、奖品、纠错；
- 键盘顺序、焦点恢复、错误关联、减少动态效果；
- 长标题、长 URL、长数字和移动端横向溢出；
- 外部链接和返回路径。

所有失败必须记录：

- 页面和宽度；
- 复现步骤；
- 截图或日志；
- 严重度；
- 修复提交；
- 复验结果。

### F4. 性能与故障演练

最低场景：

- 首页和四频道并发读取；
- OPC 下单限速和幂等；
- 20 个并发后台/聚合写入；
- Inbox 连续积压和恢复；
- Vault 或 SiC 提供方超时；
- GitHub 限流；
- RDS 短暂断开；
- Worker 被杀死后租约恢复；
- 回滚到上一发布版本。

建议上线阈值：

- 公共页面无持续 5xx；
- 正常负载下 p95 TTFB 和资源使用量留有至少 50% 余量；
- 队列能在一次上游故障后于约定窗口内清空；
- 回滚不会丢失订单、付款或报名状态。

## 5. 13 个工作日排期

外部申请在第 0 天启动，并与工程工作并行。

| 日期 | 核心目标 | 当日必须产出的证据 |
| --- | --- | --- |
| 第 0 天 | 发布冻结、负责人、域名/备案/支付/RDS 申请 | 冻结清单、负责人表、外部申请编号 |
| 第 1 天 | 已修复 Frontier 条款冲突与生产业务开关；由负责人确定真实奖励 | 统一规则、门禁测试、奖励确认 |
| 第 2 天 | 已修复文档门禁和来源统计；复核渐进发布口径 | `docs:check` 通过、范围确认记录 |
| 第 3 天 | 逐项审核首批可履约 OPC 服务；游骑兵可保持诚实空状态 | 首批正式目录修订、空状态验收 |
| 第 4 天 | RDS、迁移、生产密钥、独立后台域名 | 迁移记录、密钥清单、Passkey 登录测试 |
| 第 5 天 | Nginx、Web、Worker、Frontier Timer、监控 | 安装记录、端口检查、首个测试告警 |
| 第 6 天 | 支付宝生产配置与订单基础演练 | PC/WAP 订单、通知和主动查询证据 |
| 第 7 天 | 支付退款/异常演练；数据保留与审计 | 完整支付演练报告 |
| 第 8 天 | Vault/SiC 真实基线和榜单新鲜度 | 来源覆盖表、内容抽检记录 |
| 第 9 天 | 调度失败、模型切换、队列积压恢复 | 故障演练报告、告警记录 |
| 第 10 天 | 四宽度浏览器、键盘、焦点和错误验收 | QA 矩阵、缺陷清单 |
| 第 11 天 | 备份恢复、压力、代理/IP 绕过与回滚 | 恢复报告、压测报告、回滚记录 |
| 第 12 天 | 预发布冻结和全量回归 | 所有命令通过、P0=0 |
| 第 13 天 | Go/No-Go、发布、30–60 分钟观察 | 四方签字、发布记录、首轮健康报告 |

如果任一外部关键依赖未完成，对应日期顺延，但其他并行工作继续。

## 6. 每日发布看板

使用四列即可，不建立复杂项目系统：

| P0 事项 | 负责人 | 截止日 | 证据/阻塞 |
| --- | --- | --- | --- |
| Frontier 奖励和规则 | 产品/法律 | D1 |  |
| 条款与隐私签字 | 法律 | D3 |  |
| ICP 备案 | 运营 | 外部 |  |
| OPC 首批真实可履约目录 | 产品/运营 | D3 | 不设数量下限 |
| 游骑兵渐进发布 | 运营 | 上线后持续 | 0 份允许上线，不得虚构 |
| RDS 与迁移 | SRE/工程 | D4 |  |
| 备份恢复 | SRE | D11 |  |
| 支付安全关闭态 | 工程/运营 | D3 | 后续接入另行排期 |
| Passkey 与恢复演练 | 安全/工程 | D4 |  |
| 生产密钥与轮换 | 安全 | D5 |  |
| Vault/SiC 基线 | 内容/工程 | D8 |  |
| Worker/Timer/监控 | SRE | D5 |  |
| 浏览器验收 | QA/工程 | D10 |  |
| 回滚演练 | SRE/工程 | D11 |  |

任何事项超过截止日必须写明：

- 阻塞者；
- 下一次可行动时间；
- 是否影响正式发布日期；
- 是否存在安全的功能关闭方案。

## 7. 发布前唯一命令集

在最终生产变量和目标数据库下执行：

```powershell
npm.cmd run docs:check
npm.cmd run lint
npm.cmd run typecheck
npm.cmd test
ruff check collector
python -m unittest discover -s collector/tests -p "test_*.py"
npm.cmd run build
npm.cmd run deploy:check
npm.cmd run db:migrate
npm.cmd run test:postgres:integration
npm.cmd run test:pipeline:e2e
npm.cmd run test:acquisition:e2e
```

要求：

- 所有命令 exit code 为 0；
- 不使用本地预览存储；
- E2E 使用隔离的生产同构环境；
- 结果保存为带日期的发布证据；
- 任何跳过项都自动判定 No-Go。

## 8. 最终 Go/No-Go 清单

### 产品与业务

- [ ] Vault、SiC 有真实上线基线；
- [ ] OPC 后台发布闭环通过；所有公开服务真实、完整、可履约，未完成条目保留草稿；
- [ ] OPC 在线付款明确关闭且不收集订单信息；若决定同步开放，则真实支付闭环必须通过；
- [ ] Frontier 奖励、赛季和规则一致；
- [ ] 游骑兵为诚实空状态，或只展示已授权真实档案；
- [ ] 公开页面无演示数据和“待公布”。

### 数据与安全

- [ ] PostgreSQL 迁移和集成测试通过；
- [ ] 备份恢复通过；
- [ ] Passkey、恢复流程和独立后台通过；
- [ ] 密钥轮换通过；
- [ ] 联系方式加密和保留清理通过；
- [ ] 公网无法访问内部接口、Node 端口和后台源站。

### 运行与质量

- [ ] `docs:check`、Lint、类型、全部测试、构建、deploy check 通过；
- [ ] 两套 E2E 通过；
- [ ] 四宽度与键盘验收通过；
- [ ] 压力、积压、故障、回滚演练通过；
- [ ] 告警能够真实触发并送达。

### 合规与签字

- [ ] 条款、隐私、退款、顾问和奖品规则完成复核；
- [ ] ICP 备案号和官方查询链接已展示；
- [ ] 产品签字；
- [ ] 工程签字；
- [ ] 运营签字；
- [ ] 安全/隐私签字。

## 9. 发布当天步骤

1. 冻结主分支和生产配置；
2. 备份当前数据库；
3. 确认迁移无待执行项；
4. 部署已验收构建；
5. 执行公开页面、后台、支付、采集和健康检查烟雾测试；
6. 开启 Worker 与 Frontier Timer；
7. 确认首轮内容、榜单和队列健康；
8. 逐步开放订单和 Frontier 写入口；
9. 观察 30–60 分钟；
10. 无 P0/P1 事故后宣布上线；
11. 有数据损坏、支付异常、认证绕过或持续 5xx 时立即回滚。

## 10. 上线后 72 小时

重点观察：

- 支付订单和异步通知；
- Inbox 积压与 quarantine；
- 内容新鲜度和错误摘要；
- GitHub 限流与 Frontier Tick；
- RDS 连接、容量和备份；
- 后台异常登录；
- 用户纠错和退款请求；
- 页面 5xx、TTFB 和移动端错误。

节奏：

- 上线后 1 小时第一次复盘；
- 24 小时检查支付、采集、备份和告警；
- 72 小时形成上线稳定性报告；
- 72 小时内只修复事故和明显错误，不立即开启新功能周期。

## 11. 推荐的第一步

今天立即完成以下五件事：

1. 指定发布负责人和五个角色的实际承担人；
2. 冻结 Frontier 奖励与规则，修复条款冲突；
3. 同时启动 ICP、支付宝、RDS、域名和 TLS 外部流程；
4. 建立生产环境变量清单，但不在聊天、工单正文或 Git 中传递密钥；
5. 把 55 项上线门禁转成每日看板，每一项补负责人、日期和证据链接。

完成这五项后，项目才真正进入“向上线收敛”的状态。
