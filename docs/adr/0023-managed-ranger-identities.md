---
type: adr
status: accepted
updated: 2026-08-16
---

# ADR-0023：顾问身份随 OPC 目录受控发布

## 背景

ADR-0011 首版把十种顾问身份编译为静态分类法，避免后台成为任意栏目 CMS。真实运营需要增删、改名和排序顾问身份；继续使用中文名称作为顾问归属键，会让改名、删除、历史快照和跨模块读取产生无法稳定对齐的引用。

## 决定

顾问身份成为 OPC 服务目录中的结构化实体，使用不可随公开名称变化的稳定 ID，并与顾问档案保存在同一草稿、当前发布快照和历史发布快照中。每个身份同时保存公开名称和类别 `description`；类别说明定义整个身份的专业支持性质，不从顾问标签、介绍或当前人数派生。每位游骑兵只引用一个 `identityId`；身份首次发布后锁定稳定 ID，受保护后台只编辑公开名称、类别说明和顺序。空身份可以删除；仍被顾问引用的身份必须先在同一草稿中完成顾问迁移或移除，校验层拒绝任何孤立引用。

顾问身份继续通过 ADR-0011 的完整目录保存、乐观版本、发布前校验、最近再认证、原子发布和不可变审计通道管理，不建立独立分类 API、独立表或第二事实源。三个 OPC 一级入口、基础设施浏览分组和专项专业领域仍由规格与代码控制，后台不能创建 OPC 之外的任意栏目。

OPC 服务目录状态和 seed 使用 schema v2。读取层必须按显式 `schemaVersion` 把 schema v1 的固定中文身份映射成稳定 ID，并同时转换草稿、当前发布目录和全部历史快照。为保持当前和上一 release 的回滚窗口，本次分类说明采用 v2 加法扩展：只在读取缺少整组 `serviceCategoryDescriptions` 的既有持久化 v1/v2 目录时，按受控初始分类和身份 ID 确定性补入说明；后台完整写请求缺少 `serviceCategoryDescriptions`、`rangerIdentities` 或任何身份说明时必须拒绝，不能静默补全。下一次目录写入仍产生带完整说明的 v2。状态文档序列化器继续在 v2 顾问记录上派生一个非权威 legacy `identity` 影子值；当前代码、API 与业务校验只读取 `identityId`，影子值不建立第二事实源。生产继续使用 `vault2077_state_documents` 中同一个 `opc-service-catalog` JSONB 文档，不增加平行 SQL 字段；上一 release 回滚期间必须冻结 OPC 后台写入，避免旧写路径丢弃加法字段。

## 后果

- 身份改名或修改类别说明不改变顾问归属，身份排序决定公开左侧导航和空席顺序，真实顾问书脊仍遵循 `rangers[]` 的发布顺序。
- 删除只影响新发布快照；既有历史快照继续保存当时的身份定义和顾问归属。
- 初始十种身份只是 schema v1 的兼容基线，不再是永久冻结的产品分类法。
- 回滚到只识别固定中文身份的上一 release 时必须冻结 OPC 后台写入；自定义身份仅通过兼容影子映射维持公开目录可读，恢复新 release 后仍以 v2 `identityId` 和身份集合为准。
- ADR-0011 中“十个游骑兵身份只能修改规格和代码”的决定由本 ADR 取代，其余结构化目录边界继续有效。
