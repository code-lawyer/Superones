# Vault2077 上线负责人必办事项

日期：2026-07-30
更新：2026-07-31（工程修复后）。基础设施规格和原生 Passkey 管理员方案以 `Vault2077-Production-Deployment-Plan-2026-07-31.md` 为最新执行基线。
适用范围：只列必须由项目负责人申请、签约、购买、授权或作业务决定的事项。代码修复、门禁、文档与本地验证不重复分配给负责人。

## 1. 已由工程侧完成

- OPC 服务和游骑兵改为后台渐进发布，首发不设条目或人数下限；
- Frontier 不再硬编码“奖励待公布”，生产未确认奖励时不显示 LIVE、不渲染表单、API 拒绝写入；
- `/terms` 已与当前冠军、随机奖池和退出规则对齐；
- ICP 页脚占位已移除，真实备案号配置后自动展示并链接工信部；
- 已补 robots、sitemap、manifest；
- SiC 来源统计、3 份来源审计和文档索引已修复；
- 后台 OPC、Frontier、订单成功写入与安全审计已绑定同一 PostgreSQL 事务；
- bootstrap 导入已改为保留生产新数据、只合并缺失种子的幂等模式；
- 事件簿独立分页、SiC 显式故障态、内容流水线 E2E 和 CI 发布门禁已补齐；
- 当前类型检查与 281 项 Node 测试通过；最终全量验证结果以本轮修复报告为准。

## 2. 执行总表与提交方式

先决定发布范围：

- **只读首发（推荐）**：保持 `VAULT2077_OPC_PAYMENTS_ENABLED=false` 和 `VAULT2077_FRONTIER_WRITES_ENABLED=false`，可暂缓支付宝和真实赛季奖励；其余基础设施、Passkey、法律、内容、监控、备份和人工验收仍必须完成。
- **全功能首发**：除上述共同门禁外，还必须完成支付宝真实小额支付/退款与 Frontier 真实奖励/报名/结算演练。

| 编号 | 你要交付的结果 | 只读首发 | 全功能首发 | 给工程侧的证据（不得含秘密） |
| --- | --- | --- | --- | --- |
| 2.1 | 真实 Frontier 奖励和发布授权 | 可延后 | 必须 | 奖励公开文案、负责人、后台发布截图 |
| 2.2 | DNS、TLS、ICP 公开检查 | 必须 | 必须 | 域名、证书有效期、备案查询截图 |
| 2.3 | 支付宝商家应用和真实演练 | 可延后 | 必须 | APPID 后四位、订单号、退款单号、验收结论 |
| 2.4 | 服务器、RDS、网络、备份恢复 | 必须 | 必须 | 地域、规格、恢复演练时间、RPO/RTO |
| 2.4A | OSS 媒体 Bucket 与最小权限 RAM | 必须 | 必须 | Bucket/媒体域名、策略摘要、撤回清理结果 |
| 2.5 | 两枚 Passkey 与离线恢复演练 | 必须 | 必须 | 凭证数量、演练时间、结果；不交恢复码 |
| 2.6 | Secret、GitHub 与双编辑模型 | 必须 | 必须 | 变量名完成表、模型名、容量/切换报告；不交值 |
| 2.6A | 生产 bootstrap 与内容基线 | 必须 | 必须 | manifest 校验、导入计数、抽检记录 |
| 2.7 | 法律和业务文本签字 | 必须 | 必须 | 签字版本号、日期、确认人 |
| 2.8 | 发布、告警和事故负责人 | 必须 | 必须 | RACI/通讯录、测试告警记录 |
| 2.9 | 浏览器、移动端与可访问性验收 | 必须 | 必须 | 测试矩阵、截图、P0/P1 清零证明 |
| 2.10 | 纠错处置产品决策 | 必须决定 | 必须决定 | 选项 A/B 与批准人；若选 B 再排实现 |

每完成一项，只提交“结果、时间、负责人、证据位置和是否通过”。私钥、Token、数据库密码、恢复码、模型 Key、完整连接串不得进入聊天、工单、截图或 Git。

### 负责人最新确认

- 发布范围：**全功能首发**；支付与 Frontier 写入都必须在切流前通过。
- 应用服务器与 RDS：资源已开通；尚待生产变量注入、迁移、备份恢复和网络验收。
- OSS：资源已开通；尚待 RAM 最小权限、媒体域名、上传/撤回/版本清理验收。
- 法律文本与签字：负责人确认已完成；工程侧只需核对生产变量和公开页面一致。
- 发布/告警负责人：负责人确认已完成；切流前仍需保存一次真实测试通知记录。
- 支付宝：负责人确认商家侧事项已完成；切流前仍需生产变量门禁和受控真实支付/退款证据。
- 纠错合同：选择 **A（不可变纠错）**；当前代码和规格已满足，不建设逐条隐藏、恢复或事件重算后台。
- 浏览器验收：负责人原计划上线后完成；工程结论是全功能首发的 P0 路径必须移到切流前，上线后只能做补充观察。
- 生产 bootstrap：工程侧负责执行；前提是生产 RDS 配置以 Secret 方式进入可执行环境，并确认已有可恢复备份。

## 3. 你必须完成的事项

### 2.1 在后台发布 Frontier 真实奖励

**原因：** 奖励是公开活动承诺，代码不能替你决定真实价值、提供者和履约责任；未确认时系统必须保持关闭，避免构成虚假活动宣传。

工程侧已固定：对外提供主体表述为“边境计划管理局”，实际法律责任主体为上海睿诚明达咨询管理有限公司；依法归属于获奖者的税费由获奖者承担，运营主体依法负有扣缴或申报义务时仍须履行；赛季结束后一周内开放奖励确认和发放。每赛季只需登录后台，在“本赛季官方奖励”中填写真实奖励，保存草稿，重新认证并发布；无需重新部署。

环境级 `VAULT2077_FRONTIER_WRITES_ENABLED=true` 只作为长期总开关和紧急关闭手段。验收：页面显示 LIVE 和已发布奖励，完成一笔真实仓库报名、挑战文件验证和排行榜更新。

### 2.2 已完成：域名与 ICP 备案

**原因：** 工程仓库只能保存已确认备案文案，不能证明 DNS、证书和工信部公开查询在真实生产域名上有效。

- 公开域名：`superones.top`；
- 备案号：`沪ICP备2026003401号-1`；
- 工程侧已写入公开页脚、配置示例、生产门禁与 Nginx 模板。

你只需在 DNS 和服务器生产环境中使用同一域名与备案号，并在首次发布后检查工信部查询结果和页脚完全一致。

官方依据与查询入口：

- [工信部《非经营性互联网信息服务备案管理办法》](https://www.miit.gov.cn/zcfg/xxtxl/art/2024/art_7e48434c08c24131b4b7eecfca5b2b6c.html)
- [工信部备案查询系统](https://beian.miit.gov.cn/)

验收：备案号可在官方系统查询，公开站底部显示同一备案号并链接查询系统。

### 2.3 后续完成支付宝商家签约与应用上线

**原因：** 商户实名认证、应用审核、密钥托管、真实扣款与退款都发生在你的支付宝和生产账户中，工程侧无法代办或接触私钥。

操作：

1. 使用完成认证的商家主体创建网页/移动应用；
2. 开通电脑网站支付、手机网站支付，或明确只开其中一种；
3. 完成应用上线、收款账户和域名审核；
4. 用 RSA2 生成应用密钥对，上传应用公钥并取得支付宝公钥；
5. 将 APPID、商户 PID、应用私钥、支付宝公钥通过服务器 Secret 管理注入；
6. 通知地址使用 `https://你的公开域名/api/opc/alipay/notify`；
7. 先在预发布演练，再做一笔受控真实小额订单和退款。

需要配置的变量与完整步骤见 `docs/Vault2077-Deployment-Configuration-Manual.md` 第 5 节。

接入前生产配置保持 `VAULT2077_OPC_PAYMENTS_ENABLED=false`；系统只展示服务目录，不渲染订单表单、不收集联系人，订单 API 返回 503，因此支付宝延后不再阻塞只读正式站。接入并完成演练后再将开关改为 `true`。

验收：PC/WAP 跳转、异步通知、主动查询、重复通知幂等、金额不一致拒绝、完成、取消和退款全部留有订单号与审计证据。

### 2.4 购买并配置生产基础设施

**原因：** RDS、网络、备份策略和恢复能力是生产数据是否可恢复的事实，不可由本地文件或模拟测试替代。

你需要开通：

- 已确定的公开域名 `superones.top` 和建议后台域名 `admin.superones.top`；
- 阿里云境内轻量应用服务器，最低按 2 核 2G 起步；
- 同账号、同地域的 RDS PostgreSQL 17 基础版，初始存储 20 GB，先按量付费，上线稳定 7～14 天后转包月；
- 轻量服务器到 RDS 所在 VPC 的内网互通；
- TLS 证书、DNS、安全组、自动备份、日志备份/PITR、删除保护和基础监控。

必须作出的选择：

- 阿里云具体地域；
- RPO/RTO；
- 告警接收人和通知渠道；
- 运维 SSH 允许来源。

验收：数据库不对公网开放，Node 只监听回环，公网只开放 80/443；迁移和 PostgreSQL 集成测试通过，并从真实备份恢复到隔离实例完成读取验证。

具体步骤：

1. 在选定地域创建 2 核 2G 起步的应用服务器和 PostgreSQL 17、20 GB RDS，并建立内网互通；安全组只允许应用服务器访问 RDS。
2. 开启自动备份、日志备份/PITR 和删除保护，写下目标 RPO/RTO；不要只保留默认值而不确认业务是否接受。
3. 安装 Node、Nginx、systemd 单元与定时器；用仓库模板替换域名、证书、工作目录和环境文件路径。
4. 执行数据库迁移，导入非敏感基线；再从一个真实备份恢复到隔离实例，核对内容、订单、会话和审计表可读。
5. 从公网扫描服务器 IP、公开域名和管理域名：仅 80/443 开放，Node 端口、RDS、worker 和内部处理路由不可达。
6. 记录实例 ID、地域、规格、备份策略、恢复开始/结束时间和结果，但隐藏公网 IP、账号与连接串中的秘密部分。

### 2.4A 建立 OSS 游骑兵媒体链路

**原因：** 真实 Bucket、RAM 权限、媒体域名、对象版本和撤回删除只能在你的阿里云账户中验证；头像授权撤回若无法彻底清理，会形成隐私和合规风险。

具体步骤：

1. 在与应用相同地域创建独立私有 OSS Bucket，启用版本控制；不要复用日志、备份或其他业务 Bucket。
2. 创建仅允许该 Bucket 必要读写、列举和删除版本的 RAM 身份；禁止控制台管理、跨 Bucket 和账号级权限。
3. 绑定 `media.superones.top` 或最终媒体域名，配置 HTTPS；CORS 只允许真实公开/管理来源和实际需要的方法。
4. 通过 Secret 注入 OSS endpoint、region、bucket、access key 与公开媒体 origin；运行 `npm run deploy:check`。
5. 上传一张测试头像，替换一次，确认旧对象保留策略；执行授权撤回，确认所有版本与删除标记被永久清理；再运行孤儿对象清理演练。
6. 保存对象 key 的脱敏记录、删除前后版本数量、清理命令退出码和媒体 URL 失效证据。

验收：上传、替换、公开读取、30 天保留策略、立即撤回、版本/删除标记清理和七天孤儿清理全部通过；RAM 无法访问其他 Bucket。

### 2.5 注册生产管理员 Passkey

**原因：** WebAuthn 凭证绑定真实 HTTPS 来源、RP ID 和硬件认证器；本地自动测试不能证明你的真实设备、恢复介质和源站防绕过有效。

不再要求购买阿里云 IDaaS，也不使用 GitHub 或 Cloudflare 处理管理员身份。唯一生产 owner 邮箱仍为 `lanzhouda@163.com`。项目内原生 WebAuthn/Passkey、可撤销会话、恢复码和 SSH 本地恢复流程已经实现；你只需在目标 HTTPS 域名用真实设备完成首次注册和恢复演练。

部署到真实 HTTPS 域名后，你需要：

1. 通过服务器本地维护命令生成短时、一次性的注册令牌；
2. 在 `https://admin.superones.top` 为 `lanzhouda@163.com` 注册 Passkey；
3. 建议注册两个独立认证器，避免手机或电脑丢失后失去后台；
4. 将一次性恢复码离线保存，不放入服务器、聊天或 Git；
5. 实际验证登录、注销、五分钟再认证、单会话撤销和恢复流程。

验收：匿名写接口为 `401`；唯一 owner 可通过 Passkey 登录；伪造挑战、重放认证、公开域名和 Node 端口均无法进入后台；注销、五分钟再认证、凭证撤销和恢复生效。

### 2.6 提供生产凭证与模型选择

**原因：** 采集、编辑、签名、加密和数据库属于不同信任边界；密钥复用会让单点泄露扩大为全系统失守，模型容量不足会导致内容更新持续降级。

你需要在秘密管理中配置：

- 一个只读公开仓库用途的 GitHub Token；
- Vault 编辑模型的 base URL、API key、模型名；
- SiC 编辑模型的 base URL、API key、模型名；
- 8 个彼此独立的服务密钥/密钥环；
- 生产数据库连接串。

生成方式：使用密码管理器或云密钥管理生成至少 32 字节随机值；每个用途使用不同值；两个密钥环使用带活动 key ID 的 JSON 对象。变量清单以 `.env.example` 和部署手册为准。

具体步骤：

1. 按 `.env.example` 逐项建立生产 Secret 条目；至少 32 字节随机值，每个用途独立，密钥环使用活动 key ID + 多版本 JSON。
2. Vault 和 SiC 编辑通道分别选定主模型、受控后备模型、超时、并发和每轮预算；不要让两个通道无意共享同一故障域。
3. GitHub Token 只授予读取公开仓库所需权限；记录到期/轮换负责人。
4. 在预发布跑一次完整四通道采集与编辑，记录请求数、成功率、P95 延迟、429/5xx、预算消耗和后备切换结果。
5. 分别模拟主模型限流、超时和错误，确认只在允许错误上切换、预算耗尽时失败可见、上一成功快照不被空结果覆盖。
6. 在 `NODE_ENV=production` 下运行 `npm.cmd run deploy:check`，审查输出后只提交变量名完成表和 0 error 结论。

验收：生产门禁为 0 error，输出无秘密；双编辑通道容量和故障切换有实测记录；密钥轮换责任人和日期明确。

### 2.6A 执行生产 bootstrap 与内容基线确认

**原因：** 仓库种子已通过完整性校验，但最终导入目标、真实数据时间和公开内容抽检必须在生产数据库完成；错误基线会直接成为公开事实。

具体步骤：

1. 在导入前创建 RDS 备份或快照，记录当前内容/SiC/榜单计数。
2. 运行 `npm run bootstrap:verify`，确认 manifest 哈希和计数一致。
3. 运行 `npm run bootstrap:import -- --confirm`；该命令现在只合并缺失内容并保留更新的生产记录，可安全重跑。
4. 立即再次运行同一命令，确认公开计数没有重复增长；抽查至少 20 条资讯、10 条路边社、全部事件、每个 SiC 分组和五个榜单。
5. 检查标题、原文链接、时间、来源归属、翻译/摘要、事件引用 `[n]` 与路由；不真实或越界内容先关闭发布，不直接改数据库。
6. 保存两次导入计数、manifest hash、抽检表和异常处置记录。

验收：二次运行幂等；无重复、无种子覆盖生产新数据、无错误来源归类；抽检异常为零或已有明确关闭/回滚记录。

### 2.7 补齐营业执照字段并完成法律签字

**原因：** 法律责任、退款义务、版权、税费和顾问关系属于经营者决策，代码审查不能替代有权责任人的确认。

工程侧已形成并接入：

- `/terms` 使用与服务条款；
- `/privacy` 隐私政策；
- `/legal` 经营者信息；
- `/legal/opc-service-rules` OPC 服务、取消与退款规则；
- `/legal/frontier-rules` 边境计划活动规则；
- `/legal/ranger-notice` 游骑兵名录说明。

营业执照字段、法律联系邮箱和生效日期已于 2026-07-30 提供并写入工程配置。部署时直接使用 `.env.example` 中已确认的公开值，不要改写简称或格式。

然后请有资格的中国大陆法律顾问或由承担风险的负责人正式确认：

- 使用条款、隐私说明、生效日期和联系渠道；
- OPC 取消、变更、退款、第三方费用和消费者权利；
- Frontier 奖励、随机奖品、税费、争议和资格取消；
- 内容摘要、来源链接、版权、纠错与删除请求；
- 游骑兵为独立顾问，Vault2077 不作为双方交易一方的责任边界；
- 联系方式保存与删除规则。

验收：公开文本有版本、生效日期和负责人签字；产品实际行为与条款逐项一致。

### 2.8 指定发布与告警负责人

**原因：** 没有明确接收人和响应时限的监控等于没有监控；故障发生后不能临时决定谁处理支付、数据、安全和内容问题。

至少明确以下职责由谁承担：

- 最终 Go/No-Go；
- 支付与退款；
- 内容与来源异常；
- 数据库、备份和恢复；
- 安全事件与后台身份；
- 7×24 告警的首个接收人。

验收：每个告警有阈值、接收人、处理步骤和一次真实测试通知记录。

### 2.9 完成人工浏览器验收

**原因：** 真实浏览器、设备字体、触控、Passkey、支付跳转和屏幕阅读器行为无法由本地源码测试完整证明。

当前自动浏览器受本机安全策略限制，这一项需要你本人或 QA 在可访问的预发布域名执行。

在 360、768、1280、1440 四个宽度验证：

- 首页、导航、四频道和空/错/加载状态；
- OPC 筛选、详情、下单、支付返回和后台订单；
- SiC 展开、榜单、翻面、复制和外链；
- Frontier 规则、报名、验证、捐献和排行榜；
- 后台登录、再认证、目录发布、奖品和纠错；
- 键盘顺序、焦点恢复、错误关联、横向溢出和减少动态效果。

每个失败记录页面、宽度、步骤、截图、严重度和复验结果。P0/P1 未清零不得上线。

### 2.10 确认纠错后的内容处置规则

**原因：** 当前“只保存纠错报告”的不可变策略与“后台隐藏、恢复、移入/拆分事件”是两套不同产品合同；工程侧不能擅自扩大管理员删除或改写公开历史的权力。

请二选一并书面确认：

- **A：首发保持不可变纠错（推荐）**。后台只接收、审阅和关闭报告；确认错误后通过新的受审计发布批次修正，不增加逐条隐藏/恢复/重算按钮。优点是权限面小、可直接首发；缺点是紧急下架速度依赖工程操作。
- **B：建设可逆 moderation 层**。按下述规则实现隐藏、恢复、事件重算、移入/拆分和完整前后快照审计。优点是运营自主；缺点是需要新增数据模型、权限、后台界面、回归测试和发布演练，选择后应将其列为上线前工程工作。

现有后台能保存并关闭纠错报告，但内容库尚无可逆的人工修订层。请确认以下推荐规则：

- 隐藏资讯时保留原始记录和审计，只从公开读取中移除，并从关联事件的公开证据中移除；
- 事件失去最低有效证据后自动转为隐藏，等待人工重算；
- 恢复资讯时不自动恢复旧事件关系，先进入待重算状态；
- “移入事件”和“拆分事件”必须记录修订前后关系、操作者、理由和时间；
- 来源失效只标记来源状态，不篡改历史原文。

你只需确认或修改这些业务语义，不需要设计数据库。确认后工程侧可实现 moderation 数据层、后台动作、审计和测试。

## 4. OPC 与游骑兵的上线后操作

OPC：

1. 在后台创建或修改草稿；
2. 填全真实价格、范围、排除项、材料、成果、验收、周期和限制；
3. 由实际交付负责人复核；
4. 发布新修订；
5. 用公开页面检查详情与下单；
6. 不完整条目继续保留草稿。

游骑兵：

1. 取得本人书面授权；
2. 核验职业资料和公开联系方式；
3. 填写资料更新时间、核验日期和撤销联系人；
4. 后台逐份发布；
5. 测试公开详情和联系入口；
6. 授权撤回时立即下架并保留审计记录。

## 5. 交回工程侧后我可以继续完成

当你完成上述申请和决定后，只需提供非秘密的确认结果，以及让工程环境能安全读取 Secret 的方式。我可以继续：

- 根据真实奖励完成 Frontier 开放与端到端验证；
- 在目标 PostgreSQL 执行迁移、集成测试和基线校验；
- 验证 Nginx/systemd 配置、生产门禁、健康检查和告警；
- 陪同完成支付宝测试订单、异常路径和退款演练；
- 运行全量命令、四频道内容基线检查并更新最终 Go/No-Go 报告。

任何私钥、Token、数据库密码和模型 API Key 都不要粘贴到聊天、文档或 Git；应通过云 Secret、受限环境文件或密码管理器注入。
