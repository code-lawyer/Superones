# Vault2077 上线负责人必办事项

日期：2026-07-30
更新：2026-07-31。基础设施规格和免费管理员方案以 `Vault2077-Production-Deployment-Plan-2026-07-31.md` 为最新执行基线。
适用范围：只列必须由项目负责人申请、签约、购买、授权或作业务决定的事项。代码修复、门禁、文档与本地验证不重复分配给负责人。

## 1. 已由工程侧完成

- OPC 服务和游骑兵改为后台渐进发布，首发不设条目或人数下限；
- Frontier 不再硬编码“奖励待公布”，生产未确认奖励时不显示 LIVE、不渲染表单、API 拒绝写入；
- `/terms` 已与当前冠军、随机奖池和退出规则对齐；
- ICP 页脚占位已移除，真实备案号配置后自动展示并链接工信部；
- 已补 robots、sitemap、manifest；
- SiC 来源统计、3 份来源审计和文档索引已修复；
- Lint、类型检查、文档检查与 189 项 Node 测试通过。

## 2. 你必须完成的 P0

### 2.1 在后台发布 Frontier 真实奖励

工程侧已固定：对外提供主体表述为“边境计划管理局”，实际法律责任主体为上海睿诚明达咨询管理有限公司；依法归属于获奖者的税费由获奖者承担，运营主体依法负有扣缴或申报义务时仍须履行；赛季结束后一周内开放奖励确认和发放。每赛季只需登录后台，在“本赛季官方奖励”中填写真实奖励，保存草稿，重新认证并发布；无需重新部署。

环境级 `VAULT2077_FRONTIER_WRITES_ENABLED=true` 只作为长期总开关和紧急关闭手段。验收：页面显示 LIVE 和已发布奖励，完成一笔真实仓库报名、挑战文件验证和排行榜更新。

### 2.2 已完成：域名与 ICP 备案

- 公开域名：`superones.top`；
- 备案号：`沪ICP备2026003401号-1`；
- 工程侧已写入公开页脚、配置示例、生产门禁与 Nginx 模板。

你只需在 DNS 和服务器生产环境中使用同一域名与备案号，并在首次发布后检查工信部查询结果和页脚完全一致。

官方依据与查询入口：

- [工信部《非经营性互联网信息服务备案管理办法》](https://www.miit.gov.cn/zcfg/xxtxl/art/2024/art_7e48434c08c24131b4b7eecfca5b2b6c.html)
- [工信部备案查询系统](https://beian.miit.gov.cn/)

验收：备案号可在官方系统查询，公开站底部显示同一备案号并链接查询系统。

### 2.3 后续完成支付宝商家签约与应用上线

操作：

1. 使用完成认证的商家主体创建网页/移动应用；
2. 开通电脑网站支付、手机网站支付，或明确只开其中一种；
3. 完成应用上线、收款账户和域名审核；
4. 用 RSA2 生成应用密钥对，上传应用公钥并取得支付宝公钥；
5. 将 APPID、商户 PID、应用私钥、支付宝公钥通过服务器 Secret 管理注入；
6. 通知地址使用 `https://你的公开域名/api/opc/alipay/notify`；
7. 先在预发布演练，再做一笔受控真实小额订单和退款。

需要配置的变量与完整步骤见 `docs/Vault2077-Deployment-Configuration-Manual.md` 第 5 节。

接入前生产配置保持 `VAULT2077_OPC_PAYMENTS_ENABLED=false`；系统只展示服务目录，不渲染订单表单、不收集联系人，订单 API 返回 503，因此支付宝延后不再阻塞只读正式站。接入并完成演练后再将开关改为 `true`。

验收：PC/WAP 跳转、异步通知、主动查询、重复通知幂等、金额不一致拒绝、完成、取消和退款全部留有订单号与审计证据。

### 2.4 购买并配置生产基础设施

你需要开通：

- 已确定的公开域名 `superones.top` 和建议后台域名 `admin.superones.top`；
- 阿里云境内轻量应用服务器，最低按 2 核 2G 起步；
- 同账号、同地域的 RDS PostgreSQL 17 基础版，初始存储 20 GB，先按量付费，上线稳定 7～14 天后转包月；
- 轻量服务器到 RDS 所在 VPC 的内网互通；
- TLS 证书、DNS、安全组、自动备份、日志备份/PITR、删除保护和基础监控。

必须作出的选择：

- 阿里云具体地域；
- RPO/RTO；
- 告警接收人和通知渠道；
- 运维 SSH 允许来源。

验收：数据库不对公网开放，Node 只监听回环，公网只开放 80/443；迁移和 PostgreSQL 集成测试通过，并从真实备份恢复到隔离实例完成读取验证。

### 2.5 注册生产管理员 Passkey

不再要求购买阿里云 IDaaS，也不使用 GitHub 或 Cloudflare 处理管理员身份。唯一生产 owner 邮箱仍为 `lanzhouda@163.com`。项目内原生 WebAuthn/Passkey、可撤销会话、恢复码和 SSH 本地恢复流程已经实现；你只需在目标 HTTPS 域名用真实设备完成首次注册和恢复演练。

部署到真实 HTTPS 域名后，你需要：

1. 通过服务器本地维护命令生成短时、一次性的注册令牌；
2. 在 `https://admin.superones.top` 为 `lanzhouda@163.com` 注册 Passkey；
3. 建议注册两个独立认证器，避免手机或电脑丢失后失去后台；
4. 将一次性恢复码离线保存，不放入服务器、聊天或 Git；
5. 实际验证登录、注销、五分钟再认证、单会话撤销和恢复流程。

验收：匿名写接口为 `401`；唯一 owner 可通过 Passkey 登录；伪造挑战、重放认证、公开域名和 Node 端口均无法进入后台；注销、五分钟再认证、凭证撤销和恢复生效。

### 2.6 提供生产凭证与模型选择

你需要在秘密管理中配置：

- 一个只读公开仓库用途的 GitHub Token；
- Vault 编辑模型的 base URL、API key、模型名；
- SiC 编辑模型的 base URL、API key、模型名；
- 8 个彼此独立的服务密钥/密钥环；
- 生产数据库连接串。

生成方式：使用密码管理器或云密钥管理生成至少 32 字节随机值；每个用途使用不同值；两个密钥环使用带活动 key ID 的 JSON 对象。变量清单以 `.env.example` 和部署手册为准。

验收：`NODE_ENV=production` 下 `npm.cmd run deploy:check` 为 0 error，输出中不出现任何秘密。

### 2.7 补齐营业执照字段并完成法律签字

工程侧已形成并接入：

- `/terms` 使用与服务条款；
- `/privacy` 隐私政策；
- `/legal` 经营者信息；
- `/legal/opc-service-rules` OPC 服务、取消与退款规则；
- `/legal/frontier-rules` 边境计划活动规则；
- `/legal/ranger-notice` 游骑兵名录说明。

营业执照字段、法律联系邮箱和生效日期已于 2026-07-30 提供并写入工程配置。部署时直接使用 `.env.example` 中已确认的公开值，不要改写简称或格式。

然后请有资格的中国大陆法律顾问或由承担风险的负责人正式确认：

- 使用条款、隐私说明、生效日期和联系渠道；
- OPC 取消、变更、退款、第三方费用和消费者权利；
- Frontier 奖励、随机奖品、税费、争议和资格取消；
- 内容摘要、来源链接、版权、纠错与删除请求；
- 游骑兵为独立顾问，Vault2077 不作为双方交易一方的责任边界；
- 联系方式保存与删除规则。

验收：公开文本有版本、生效日期和负责人签字；产品实际行为与条款逐项一致。

### 2.8 指定发布与告警负责人

至少明确以下职责由谁承担：

- 最终 Go/No-Go；
- 支付与退款；
- 内容与来源异常；
- 数据库、备份和恢复；
- 安全事件与后台身份；
- 7×24 告警的首个接收人。

验收：每个告警有阈值、接收人、处理步骤和一次真实测试通知记录。

### 2.9 完成人工浏览器验收

当前自动浏览器受本机安全策略限制，这一项需要你本人或 QA 在可访问的预发布域名执行。

在 360、768、1280、1440 四个宽度验证：

- 首页、导航、四频道和空/错/加载状态；
- OPC 筛选、详情、下单、支付返回和后台订单；
- SiC 展开、榜单、翻面、复制和外链；
- Frontier 规则、报名、验证、捐献和排行榜；
- 后台登录、再认证、目录发布、奖品和纠错；
- 键盘顺序、焦点恢复、错误关联、横向溢出和减少动态效果。

每个失败记录页面、宽度、步骤、截图、严重度和复验结果。P0/P1 未清零不得上线。

### 2.10 确认纠错后的内容处置规则

现有后台能保存并关闭纠错报告，但内容库尚无可逆的人工修订层。请确认以下推荐规则：

- 隐藏资讯时保留原始记录和审计，只从公开读取中移除，并从关联事件的公开证据中移除；
- 事件失去最低有效证据后自动转为隐藏，等待人工重算；
- 恢复资讯时不自动恢复旧事件关系，先进入待重算状态；
- “移入事件”和“拆分事件”必须记录修订前后关系、操作者、理由和时间；
- 来源失效只标记来源状态，不篡改历史原文。

你只需确认或修改这些业务语义，不需要设计数据库。确认后工程侧可实现 moderation 数据层、后台动作、审计和测试。

## 3. OPC 与游骑兵的上线后操作

OPC：

1. 在后台创建或修改草稿；
2. 填全真实价格、范围、排除项、材料、成果、验收、周期和限制；
3. 由实际交付负责人复核；
4. 发布新修订；
5. 用公开页面检查详情与下单；
6. 不完整条目继续保留草稿。

游骑兵：

1. 取得本人书面授权；
2. 核验职业资料和公开联系方式；
3. 填写资料更新时间、核验日期和撤销联系人；
4. 后台逐份发布；
5. 测试公开详情和联系入口；
6. 授权撤回时立即下架并保留审计记录。

## 4. 交回工程侧后我可以继续完成

当你完成上述申请和决定后，只需提供非秘密的确认结果，以及让工程环境能安全读取 Secret 的方式。我可以继续：

- 根据真实奖励完成 Frontier 开放与端到端验证；
- 在目标 PostgreSQL 执行迁移、集成测试和基线校验；
- 验证 Nginx/systemd 配置、生产门禁、健康检查和告警；
- 陪同完成支付宝测试订单、异常路径和退款演练；
- 运行全量命令、四频道内容基线检查并更新最终 Go/No-Go 报告。

任何私钥、Token、数据库密码和模型 API Key 都不要粘贴到聊天、文档或 Git；应通过云 Secret、受限环境文件或密码管理器注入。
