target-version = "py312"
line-length = 120
exclude = [
  "collector/vendor",
]

[lint]
# Start with correctness-oriented rules. Formatting is intentionally kept out
# of the launch gate to avoid a repository-wide mechanical rewrite.
select = ["E4", "E7", "E9", "F", "I", "B"]
