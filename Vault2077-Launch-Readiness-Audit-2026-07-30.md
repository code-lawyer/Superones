# Vault2077 全功能上线准备度审计（历史快照）

> 本文记录 2026-07-30 当时的仓库状态，不应作为当前部署手册。当前执行依据为 `docs/Vault2077-Aliyun-Mainland-Production-Handoff.md`、`docs/Vault2077-Deployment-Configuration-Manual.md` 与 `docs/Vault2077-Launch-Checklist.md`。

审计日期：2026-07-30
审计对象：当前工作区（含未提交修改）
审计口径：正式生产上线，而不是本地演示、代码完成或页面可访问
范围确认：OPC 服务内容与游骑兵档案允许上线后从后台渐进增加，首发不设数量下限；公开内容真实性、后台发布可靠性和其他生产门禁不降低。

## 1. 最终结论

**结论：当前不得正式上线，不得启用真实收款、真实边境计划赛季或对外宣称四频道已经稳定运行。**

当前项目已经达到“主要产品界面和后端机制基本成形、自动化测试体系较完整”的准上线工程阶段，但尚未达到“真实业务可交付、生产环境可恢复、合规条款一致、所有关键链路经过目标环境验证”的上线阶段。

完成本轮可本地修复项及最新范围确认后，综合准备度建议更新为 **64 / 100**：

| 领域 | 准备度 | 结论 |
| --- | ---: | --- |
| 代码与自动化质量 | 90% | Lint、类型、189 项 Node 测试和文档门禁通过 |
| 公开产品功能 | 62% | 页面完整；Vault/SiC/Frontier 真实数据仍为空，游骑兵允许诚实空状态 |
| 真实业务闭环 | 42% | Frontier 奖励已支持后台逐赛季发布；OPC 在线付款按确认范围延期并安全关闭 |
| 数据与安全实现 | 68% | 代码边界较强；目标环境、轮换和恢复证据缺失 |
| 部署与运维 | 25% | 模板存在，目标服务器安装、监控、备份恢复均无证据 |
| 合规与发布签字 | 22% | 规则冲突与备案占位文案已修；真实备案号、法律复核和发布签字仍缺 |

这里的百分比只是帮助判断距离；正式上线是门禁制，任一 P0 阻塞都足以否决发布。

如果生产账号、域名、RDS、身份网关、支付签约和业务资料已经可以立即提供，预计仍需 **10–20 个有效工作日**完成配置、数据回填、规则修正、全链路演练和签字。若这些外部输入尚未确定，则无法给出可靠上线日期。

## 2. 状态定义

- **通畅**：本轮有代码、自动化测试或实际 HTTP 证据，并且不依赖缺失的生产输入。
- **部分通畅**：主流程实现存在，但真实数据、异常闭环、目标环境或人工验收未完成。
- **阻塞**：用户当前无法完成核心目标，或功能不应在生产开放。
- **未实现**：规格要求的能力缺少实际操作闭环。
- **未验证**：本轮无法取得足够证据，不能按通过处理。

## 3. 全功能通畅性矩阵

### 3.1 全站与公共页面

| 功能 | 状态 | 本轮结论 |
| --- | --- | --- |
| 首页 | 部分通畅 | HTTP 200，结构和四频道入口存在；Vault 事件、SiC 正式内容、Frontier 排名均为空，只能显示等待或空态 |
| 主导航与移动菜单 | 部分通畅 | 代码具备菜单开关、当前频道和键盘语义；本轮真实浏览器交互未完成 |
| 页脚与辅助入口 | 部分通畅 | 占位文案已移除，配置备案号后自动展示并链接工信部；真实备案号尚未提供 |
| 关于页 | 通畅 | HTTP 200，静态内容可访问 |
| 方法说明 | 通畅 | HTTP 200，静态内容可访问 |
| 数据源地图 | 通畅（代码） | HTTP 200；SiC 来源统计已与当前注册表一致 |
| 境外信息管线清单 | 通畅（代码） | HTTP 200、noindex；来源审计元数据与索引门禁已通过 |
| 隐私说明 | 部分通畅 | 页面存在并覆盖主要数据用途；仍缺业务/法律正式确认 |
| 使用与服务条款 | 部分通畅 | Frontier 获奖、随机奖池和冠军退出规则已与实现一致；仍需业务/法律正式确认 |
| 404、异常页与降级体验 | 未验证 | 构建成功，部分代码有 fail-closed；未完成浏览器级错误页和全频道降级验收 |
| SEO 基础 | 通畅（代码） | 页面 Metadata、robots、sitemap 和 manifest 已补齐；生产 origin 由部署门禁保证 |
| 四宽度响应式 | 未验证 | 有 CSS 合约测试和历史记录；本轮浏览器策略阻止 360/768/1280/1440 实测 |
| 键盘、焦点、读屏 | 未验证 | 组件代码普遍包含 label、aria、焦点回收；未完成真实浏览器全链路验收 |

### 3.2 Vault 信息流

| 功能 | 状态 | 本轮结论 |
| --- | --- | --- |
| 事件簿 | 阻塞 | 页面 HTTP 200，但当前事件数为 0 |
| 资讯瀑布 | 阻塞 | 页面 HTTP 200，但当前资讯数为 0 |
| 路边社 | 阻塞 | 页面 HTTP 200，但当前内容数为 0 |
| 事件详情 | 部分通畅 | 路由、引用、相关资讯分批加载代码存在；没有真实记录可验收 |
| 资讯详情 | 部分通畅 | 路由、原文链接、纠错入口存在；没有真实记录可验收 |
| 路边社详情 | 部分通畅 | 分流与详情实现存在；没有真实记录可验收 |
| “继续加载” | 部分通畅 | URL 驱动增量加载逻辑和单测存在；当前数据为空，未做浏览器实测 |
| 匿名纠错提交 | 部分通畅 | 无效请求正确返回 400；持久化、限速、加密邮箱测试通过 |
| 纠错处理闭环 | 未实现 | 后台只能填写说明并关闭；没有拆分事件、移入事件、隐藏错误内容等实际修正操作 |
| 内容事件晋升 | 部分通畅 | 阈值、去重、失败隔离和 E2E 通过；当前没有真实上线基线 |
| 生产失败不回退假数据 | 通畅（代码） | Vault 已有显式降级语义与测试 |

### 3.3 OPC 服务台

| 功能 | 状态 | 本轮结论 |
| --- | --- | --- |
| 三入口 | 通畅 | 基础设施、专项服务、游骑兵入口和筛选模型存在 |
| 基础设施服务 | 通畅（目录） | 当前种子有 7 项；正式上线不设数量下限，仅发布真实、完整、可履约条目 |
| 专项服务 | 通畅（目录） | 当前种子有 14 项；可在后台逐项审核和渐进发布 |
| 服务详情 | 通畅（代码） | 范围、材料、成果、验收、费用、周期和边界字段齐全 |
| 服务筛选与选择 | 部分通畅 | 组件和视觉合约测试通过；未完成本轮浏览器交互 |
| 无账号订单表单 | 通畅（关闭态） | 支付开关关闭时不渲染表单、不收集联系人，明确显示在线付款尚未开放 |
| 订单接口 | 通畅（关闭态） | 支付开关关闭时返回 503；未来只有开关和完整生产配置同时有效才接受订单 |
| 服务端价格快照 | 通畅（代码） | 接口只信任已发布服务快照，测试通过 |
| 联系方式加密 | 通畅（代码） | 密钥版本、轮换兼容和后台投影测试通过；当前没有生产密钥环 |
| 支付宝收银台跳转 | 后续功能 | SDK 与 URL 生成测试通过；真实 APPID、PID、密钥、产品签约和网关待后续接入 |
| 支付宝异步通知 | 部分通畅 | RSA2、商户、订单和金额校验测试通过；没有真实通知证据 |
| 支付主动查询 | 部分通畅 | 后台实现和审计存在；没有真实交易演练 |
| 重复通知、金额不一致、退款 | 部分通畅 | 代码和单测覆盖部分拒绝路径；完整真实支付演练缺失 |
| 支付返回页 | 部分通畅 | 页面正确声明“返回不等于到账”；没有订单查询状态展示 |
| 游骑兵公开名录 | 通畅（允许空态） | 当前正式默认名录为 0；按确认口径不阻塞上线，后续只从后台逐份发布真实授权档案 |
| 游骑兵档案详情 | 通畅（代码） | 路由和模板存在；有真实授权档案后再开放具体详情，不以虚构资料填充 |
| 服务目录后台草稿/发布 | 部分通畅 | 新增、删除、排序、校验、并发控制、发布快照和审计均已实现；未在目标 PostgreSQL 验收 |
| 订单后台处理 | 部分通畅 | 状态机、再认证、主动查询、完成/取消/退款代码存在；无真实订单链路 |

### 3.4 SiC 学院

| 功能 | 状态 | 本轮结论 |
| --- | --- | --- |
| 论文组 | 阻塞 | 页面结构存在，当前正式内容为 0 |
| 档案组 | 阻塞 | 页面结构存在，当前正式内容为 0；内部 ID 仍使用 `documents` |
| 课程组 | 阻塞 | 页面结构存在，当前正式内容为 0 |
| 播客组 | 阻塞 | 页面结构存在，当前正式内容为 0 |
| 内容展开与原文跳转 | 部分通畅 | 组件实现存在；无真实内容且未做浏览器交互 |
| GitHub 今日榜 | 通畅 | 当前有 10 条，捕获时间为 2026-07-30 |
| GitHub 周榜 | 通畅 | 当前有 20 条 |
| GitHub 月榜 | 通畅 | 当前有 20 条 |
| Hugging Face Trending | 通畅 | 当前有 20 条 |
| OpenRouter Top Weekly | 通畅 | 当前有 20 条 |
| 榜单 Top 5/10 展开、翻面、复制 | 部分通畅 | 组件和部分合约测试存在；本轮未做真实浏览器交互 |
| 榜单 last-success / stale | 通畅（代码） | 保留上次成功结果和 stale 标识的测试通过 |
| SiC 来源目录 | 通畅 | 当前注册表和目录均为 22 approved、11 retired、1 pending_review，文档门禁通过 |
| SiC 生产降级 | 部分通畅 | 读取端 catch 后显示空组；尚未统一为明确的生产降级状态 |

### 3.5 边境计划

| 功能 | 状态 | 本轮结论 |
| --- | --- | --- |
| 当前赛季页面 | 通畅（门禁） | 当前赛季奖励未从后台发布时显示 PREPARING；发布后才显示 LIVE |
| 赛季计算 | 通畅（代码） | 当前识别为 2026-Q3，季度边界测试通过 |
| 官方冠军奖励 | 部分通畅 | 后台已支持按赛季保存草稿、重新认证发布和持久化；当前赛季仍需运营填写真实奖励 |
| 报名表单 | 阻塞（生产配置） | 表单与校验存在；生产奖励/开关未确认时页面不渲染表单，API 返回 503 |
| GitHub 仓库资格检查 | 部分通畅 | URL、仓库、许可证和可运行性规则存在；目标凭证和真实故障演练缺失 |
| 挑战文件生成 | 部分通畅 | 代码、过期和重复报名恢复逻辑存在；无真实生产报名证据 |
| 挑战文件验证 | 部分通畅 | 默认分支读取和哈希匹配实现存在；无真实生产验证证据 |
| 当前排行榜 | 阻塞 | 接口 HTTP 200，但 entries 为空、updatedAt 为 null |
| 每小时 Star 更新 | 阻塞 | tick 代码和 systemd 模板存在；目标服务器未安装、无 GITHUB_TOKEN、无告警证据 |
| GitHub 失败异步回退 | 部分通畅 | 公开任务和 rankings 回传机制测试通过；未做生产故障演练 |
| 奖品捐献 | 阻塞（生产配置） | 表单和后台状态机存在；生产奖励/开关未确认时页面不渲染表单，API 返回 503 |
| 奖品确认/拒绝/撤回 | 部分通畅 | 后台实现存在，要求再认证；未在目标环境验收 |
| 结算与幂等 | 部分通畅 | 租约、失败恢复、重试与幂等单测通过；没有真实赛季结算演练 |
| 往届记录 | 部分通畅 | 页面和数据模型存在；当前无历史结果 |
| 条款一致性 | 通畅（代码） | `/terms` 已与现行排名、官方奖励、随机奖池和冠军退出规则对齐 |

### 3.6 运营后台

| 功能 | 状态 | 本轮结论 |
| --- | --- | --- |
| 本地后台登录 | 通畅（开发） | 本地返回 `local-password` 模式；仅适用于开发 |
| 生产身份网关 | 阻塞 | JWT/JWKS/issuer/audience/allowlist 校验代码和单测存在；所有真实配置缺失 |
| 独立后台域名 | 阻塞 | 代码强制独立 origin；目标域名、网关和绕过测试无证据 |
| 可撤销会话 | 通畅（代码） | 不透明令牌摘要、空闲/绝对过期、单会话撤销测试通过 |
| 再认证 | 通畅（代码） | 高风险动作要求最近 5 分钟再认证 |
| CSRF/同源防护 | 通畅（代码） | Host、Origin/Referer、自定义头和 JSON 限制测试通过 |
| 不可变审计 | 通畅（代码） | 登录、目录、订单、Frontier 操作均写审计；数据库触发器阻止更新/删除 |
| 内容状态查看 | 部分通畅 | 可读当前状态；当前真实内容为空 |
| 纠错处理 | 未实现完整闭环 | 只能关闭报告，不能执行内容层修正 |
| OPC 目录维护 | 部分通畅 | 功能较完整；未在真实网关和数据库环境验收 |
| OPC 订单处理 | 阻塞 | 后台功能存在；上游支付和真实订单未打通 |
| Frontier 管理 | 部分通畅 | 刷新 Star、奖品状态、结算存在；真实生产条件缺失 |

### 3.7 采集、处理与调度

| 功能 | 状态 | 本轮结论 |
| --- | --- | --- |
| 单一境外 workflow | 通畅 | 只有 `collect-content.yml` |
| information 通道 | 通畅（代码） | 调度、路由、合同和处理器存在；没有生产基线 |
| roadside 通道 | 通畅（代码） | HN/Lobsters canonical 策略和测试通过；没有生产基线 |
| sic 通道 | 部分通畅 | 采集器和测试通过；当前正式内容为空，注册表正在变更 |
| rankings 通道 | 通畅 | 当前 5 个榜单有实际快照 |
| 批次签名与密钥 ID | 通畅（代码） | 时间窗、重放、签名轮换、幂等测试通过 |
| 可靠投递 | 通畅（测试） | 统一采集 HTTP E2E 通过 |
| Inbox 与 Worker | 部分通畅 | 租约、重试、quarantine、恢复测试通过；目标 timer 未安装 |
| 内容管线 E2E | 通畅（本地） | 202 接收、1 批处理、事件发布成功 |
| 双编辑配置 | 阻塞（当前环境） | Vault 模型配置存在；SiC 模型配置缺失，生产门禁失败 |
| Bootstrap 上线基线 | 阻塞 | 当前 Vault 0、SiC 0，未执行真实上线基线 |
| 跨存储原子性 | 部分通畅 | 追踪矩阵承认跨存储写入可能半提交 |
| 采集健康审计 | 部分通畅 | 健康数据和审计脚本存在；当前文档/注册表不一致 |

### 3.8 数据、安全、部署与运维

| 功能 | 状态 | 本轮结论 |
| --- | --- | --- |
| PostgreSQL 生产持久化 | 阻塞 | 适配器和 5 个迁移存在；当前未配置数据库 |
| 数据库迁移 | 未验证 | 集成代码/历史证据存在；本轮没有目标 PostgreSQL |
| TLS、自动备份、PITR | 阻塞 | 无目标环境证据 |
| 真实备份恢复 | 未实现证据 | 没有日期、RPO、RTO、恢复结果 |
| 联系方式保留期清理 | 部分通畅 | 24 个月清理逻辑存在；未在目标数据库演练 |
| 密钥环和轮换 | 部分通畅 | 代码和单测通过；目标秘密管理、重加密和撤旧演练缺失 |
| CSP 与安全头 | 通畅（代码） | nonce CSP、HSTS、nosniff、frame/referrer/permissions 等已实现并测试 |
| 依赖漏洞 | 通畅 | `npm audit --omit=dev --audit-level=high`：0 个漏洞 |
| 仓库秘密扫描 | 通畅（有限扫描） | 未发现常见私钥/token 模式；`.env.local` 被忽略且未跟踪 |
| Nginx 边界 | 部分通畅 | 模板只开放必要内部路由；未做目标服务器语法、IP 绕过和伪造头测试 |
| systemd Web/Worker/Timer | 阻塞 | 模板存在；未安装、未接入退出告警 |
| 健康检查 | 部分通畅 | 受保护接口和业务健康项存在；当前密钥未配置、告警平台未接入 |
| 容量与压力测试 | 未验证 | 没有目标并发、TTFB、队列积压、模型预算和限流阈值证据 |
| 回滚/事故演练 | 未验证 | 没有日期、负责人和结果 |
| ICP 备案展示 | 阻塞（外部输入） | 页脚占位已移除并支持配置真实备案号；生产检查强制要求有效 ICP 备案号 |

## 4. 本轮验证结果

### 通过

- `npm run lint`
- `npm run typecheck`
- Node 单元测试：**191 / 191**
- Python 采集器测试：**16 / 16**
- `ruff check collector`
- `npm run build`
- 内容管线 E2E
- 统一采集 Inbox HTTP E2E
- 开发服务器资源检查：首页 200，18 个资源可用
- 19 个公共/内部页面路由在开发模式返回 HTTP 200
- 主要无效请求和未授权请求正确返回 400 / 401 / 403 / 415 / 503
- `npm audit --omit=dev --audit-level=high --registry=https://registry.npmjs.org`：0 个漏洞
- `git diff --check`
- `npm run docs:check`：**62 个 Markdown 全部通过**

### 失败或未通过

- `npm run deploy:check` 失败，共报告 **25 项生产配置错误**：
  - 无 PostgreSQL；
  - Frontier 环境总开关未明确；每赛季奖励改由后台发布；
  - OPC 付款功能开关未明确；当前可安全设为关闭；
  - ICP 备案号未配置；
  - 无支付宝生产配置；
  - 无 6 个生产服务密钥；
  - 无敏感数据与管线签名密钥环；
  - 无公开站/后台 origin；
  - 无身份网关 issuer、audience、JWKS、header、allowlist、reauth URL；
  - 无 GITHUB_TOKEN；
  - SiC 编辑模型未配置；
  - 未启用标准可信代理头。
- 当前运行数据：
  - Vault 事件：0；
  - Vault 资讯：0；
  - SiC 内容：0；
  - Frontier 报名：0；
  - Frontier 排名：0；
  - 游骑兵档案：0（按已确认的渐进发布口径，不阻塞上线）；
  - OPC 支付：不可用。
- 上线清单共有 **55 项，已签 0 项，未签 55 项**。
- 本轮真实浏览器控制被安全策略拒绝，因此响应式、键盘、焦点和交互视觉不能判定通过。

## 5. 必须先解决的 P0 阻塞

按发布顺序建议：

1. **冻结真实业务规则**
   - 修正 `/terms` 与 Frontier 当前规则冲突；
   - 确认冠军奖励、随机奖品规则、赛季状态和异常处理；
   - 完成隐私、退款、版权、顾问责任和数据保留的业务/法律签字。

2. **建立发布开关**
   - 在真实奖励、真实条款、生产数据库和 GitHub 凭证未齐全时，生产环境必须禁止 Frontier 报名和奖品捐献；
   - 在 OPC 目录没有正式发布修订或支付未配置时继续禁止下单；
   - 不允许仅靠“数据库已连接”就把 Frontier 标为 LIVE。

3. **补齐真实内容与首批可履约产品输入**
   - 执行 Vault 最近 30 天真实内容基线；
   - 为每个 approved SiC 来源生成至少一条真实合格内容；
   - OPC 与游骑兵不设首发数量下限；
   - 所有已公开 OPC 服务必须有真实价格、范围、验收和生效修订，未完成条目留在草稿；
   - 游骑兵可以保持诚实空状态，后续只发布本人授权的真实档案。

4. **保持支付关闭，后续独立验收后开放**
   - 上线阶段保持 `VAULT2077_OPC_PAYMENTS_ENABLED=false`，不收集订单信息；
   - 后续完成支付宝产品签约与生产密钥；
   - 演练创建、PC/WAP 跳转、异步通知、主动查询、重复通知、金额不一致、取消和退款；
   - 保存真实订单与审计证据。

5. **部署生产数据层**
   - 在目标 RDS 执行迁移；
   - 启用 TLS、自动备份、日志备份、PITR、删除保护和监控；
   - 从真实备份恢复到隔离实例并记录 RPO/RTO。

6. **完成生产入口和运维**
   - 配置独立公开域名和后台域名；
   - 安装身份网关、Nginx、Web、采集 Worker 和 Frontier Timer；
   - 接入进程退出、内容新鲜度、积压、quarantine、榜单 stale、模型预算和任务延迟告警；
   - 验证服务器 IP、Node 端口和其他内部接口不能被公网绕过。

7. **继续关闭工程缺口**
   - `docs:check` 与 SiC 22/11/1 来源统计已修复并通过；
   - 补齐纠错后的真实内容修正动作；
   - 解决跨存储半提交风险，或形成明确补偿与重放演练。

8. **完成发布验收**
   - 360 / 768 / 1280 / 1440；
   - 键盘、焦点、错误、加载、超时、外链、复制、弹窗和横向溢出；
   - 支付、报名、奖品、后台、降级和恢复；
   - 完成产品、工程、运营、安全/隐私四方签字。

9. **完成大陆公开站合规信息**
   - 取得并在主页底部展示 ICP 备案编号及查询链接；
   - 删除“待接入”占位信息。

## 6. 可上线范围建议

### 现在可以做

- 内部演示；
- 本地或受控预览；
- 只读展示 OPC 服务目录；
- 展示当前 SiC 五个榜单，并明确快照时间；
- 继续运行测试、内容回放和目标环境演练。

### 现在不应开放

- 真实 OPC 收款；
- 真实 Frontier 报名、捐献和赛季结算；
- 宣称 Vault/SiC 内容频道已经持续运行；
- 在没有真实授权档案时宣称游骑兵协会已有顾问网络；
- 宣称系统已经生产就绪或具备灾难恢复能力。

### 最小可公开 Beta

如需尽快公开，可考虑只读 Beta：

- 保留首页、方法、来源、OPC 目录和 SiC 榜单；
- 关闭 OPC 订单；
- 关闭 Frontier 报名与捐献；
- 对 Vault、SiC 内容空态标明“尚未开放”；
- 保留游骑兵诚实空状态，不虚构档案；
- 条款冲突和文档门禁已修复；继续完成 ICP 与部署合规。

这只能称为公开 Beta，不能称为正式上线。

## 7. 上线判定

**当前判定：NO-GO。**

下一次可以进入正式上线评审的最低条件是：

- 55 项 P0/P1 清单中所有 P0 均有负责人、日期和证据；
- `docs:check`、静态检查、全部测试、E2E、构建和 `deploy:check` 全部通过；
- 真实内容、真实支付、真实身份网关、真实 RDS、真实调度与真实告警至少完成一轮端到端演练；
- 法律条款、赛季规则、顾问责任、退款与隐私没有互相冲突；
- 浏览器与四宽度验收完成；
- 产品、工程、运营、安全/隐私共同签字。
