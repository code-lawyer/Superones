---
type: deployment-plan
status: superseded
updated: 2026-08-12
---

# Vault2077 生产部署方案（2026-08-06 更新）

> 本方案已被 `docs/Vault2077-Aliyun-Mainland-Production-Handoff.md`、`docs/Vault2077-Deployment-Configuration-Manual.md` 与 ADR-0022 取代，只保留历史基础设施决策背景。不得按本文配置旧在线支付、执行发布顺序或判断当前 Go/No-Go。

状态：历史方案（已废止）
适用域名：`superones.top`、`admin.superones.top`、`media.superones.top`
运营与收款主体：上海睿诚明达咨询管理有限公司
备案号：沪ICP备2026003401号-1

本方案取代此前关于“必须购买阿里云 IDaaS”和“RDS 规格待定”的部署假设。当前发布目标为全功能首发：支付宝和 Frontier 可以在部署阶段暂时关闭写入，但必须在真实验收后启用，才能执行公开 Go/No-Go。

## 1. 最终生产拓扑

```text
公网用户
   │ HTTPS 443
   ▼
阿里云 ECS（中国大陆）
   ├─ Nginx：superones.top / admin.superones.top
   ├─ Node.js 24 + Next.js：127.0.0.1:3000
   ├─ acquisition worker：systemd timer，每 5 分钟
   └─ Frontier tick：systemd timer
           │ TLS + 私网
           ▼
阿里云 RDS PostgreSQL 17（当前 100 GB）
   ├─ 自动快照与日志备份
   ├─ 自动备份、日志备份/PITR、删除保护
   └─ 50%/70%/80% 容量告警

Node.js ──同地域内网写入/HEAD──▶ 阿里云 OSS Standard LRS
                                      └─ media.superones.top：匿名只读处理后头像
```

首发不使用 Cloudflare、Redis、CDN、只读实例或数据库集群。2026-08-06 现网控制面确认：当前 RDS PostgreSQL 17 Basic 实例已启用日志备份，并能返回有效的本地时间点恢复区间；因此不再把“Basic 系列”本身视为迁移硬阻断。公开上线前仍必须开启删除保护、确认恢复窗口符合 RPO，并从真实时间点恢复到隔离实例验证 RTO 与数据可读性；高可用多可用区仍是后续韧性升级方向。OSS 只保存服务端处理后的公开游骑兵头像，不保存原图、授权材料、采集包或数据库备份；服务器、RDS 与 OSS 必须具备受控的同地域私网链路，RDS 不开放公网连接。

## 2. 当前资源基线与扩容规则

### 2.1 ECS

- 首发基线：2 核 2 GB；如果真实 worker/构建出现持续内存压力，再升级到 2 核 4 GB；
- 操作系统：受支持的 Linux LTS；
- 公网只开放 80/443，SSH 只允许负责人的受控来源；
- Node 只监听 `127.0.0.1:3000`；
- 服务器时间使用 NTP 自动同步。

### 2.2 RDS PostgreSQL

- PostgreSQL 17；
- 以当前实例 API 的日志备份状态和本地可恢复时间区间为准，并完成真实隔离恢复；不得只凭系列名称或“备份成功”状态声称具备 PITR；
- 当前存储 100 GB；
- 连接池初始为 4；
- 付费方式、续费和升级只按当前实例合同与预算决定，不把采购建议写成部署步骤；
- 容量按 50%/70%/80% 分级告警，扩容决策同时核对增长来源、保留清理和异常积压；
- 正常增长预计至少可使用 3～5 年；如果隔离批次异常积压，优先修复清理/处理故障，不用扩容掩盖问题。

### 2.3 游骑兵头像 OSS

- Standard LRS，与 ECS 和 RDS 同地域；
- 使用独立 Bucket 与 `media.superones.top`，首发不接 CDN；
- 匿名用户只允许读取 `rangers/*` 处理后对象，绝不允许匿名写入；
- 应用 RAM 身份只允许写入和读取 `rangers/*` 对象元数据，不授予 Bucket 管理权限；
- 开启 HTTPS、访问/流量/费用告警和误删保护；生产启用前落实无引用对象清理与本人撤回后的永久删除流程。

### 2.4 数据可靠性

- 开启自动备份、日志备份/时间点恢复、删除保护和监控；
- 上线前将一次真实备份恢复到隔离实例，验证迁移记录、业务表、关键状态文档和审计链；
- 初始验收目标：能够按受控时间点恢复，且数据库节点故障具备与选定系列一致的恢复/切换能力；
- 当前 Basic 实例已返回本地时间点恢复区间，但尚未完成隔离恢复演练；若实际恢复能力、RPO 或 RTO 不满足业务目标，再决定升级到高可用多可用区或迁移系列。

## 3. 免费管理员身份方案

生产后台不购买阿里云 IDaaS，也不依赖 GitHub 或 Cloudflare 登录。目标方案为项目内原生 WebAuthn/Passkey：

- 唯一 owner 固定为 `lanzhouda@163.com`；
- Passkey 私钥保存在负责人的设备/密码管理器中，服务器只保存公钥、凭证 ID、计数器和审计记录；
- 首次注册通过仅能从服务器本地生成、短时有效、一次性使用的引导令牌完成；
- 登录和高风险操作五分钟再认证都使用 Passkey；
- 应用会话继续使用 PostgreSQL 可撤销会话和 `__Host-` 安全 Cookie；
- 生成一次性恢复码并只保存哈希；恢复码由负责人离线保管；
- 服务器本地维护命令可以撤销全部会话并重新开放一次性注册，不提供公网“找回密码”；
- 公开域名拒绝 `/admin` 和 `/api/admin/*`，只有 `admin.superones.top` 提供后台入口。

仓库已完成原生 Passkey 门禁替换：包含一次性引导令牌、注册、登录、五分钟再认证、恢复码、凭证撤销、会话撤销和 SSH 全量恢复。后台不再依赖或接受阿里云 IDaaS OIDC 配置。部署状态仍须等待真实设备、TLS、RDS 和网络边界验收，不能仅凭本地自动化测试判定上线。

## 4. 部署执行顺序

### 阶段 A：负责人开通外部资源

1. 核对现有阿里云 ECS 的地域、系统、磁盘、续费策略、安全组和受控 SSH 入口；
2. 核对已购买的 RDS PostgreSQL 17，启用 SSL、日志备份、删除保护和时间点恢复，并从真实恢复点完成隔离恢复；若恢复能力或韧性不满足目标，再升级到高可用多可用区；
3. 创建同地域 OSS Standard LRS Bucket，配置最小权限 RAM 身份、误删保护和费用告警；
4. 核对 ECS 到 RDS VPC 的私网可达性、安全组和数据库白名单；
5. 将 `superones.top` 和 `admin.superones.top` 解析到服务器，将 `media.superones.top` CNAME 到 OSS；
6. 为三个公开主机配置有效 TLS；
7. 设置告警接收人、SSH 允许来源、RDS 备份保留策略和头像对象清理责任人。

### 阶段 B：工程侧上线前完成

1. 在目标环境用真实设备验收原生 Passkey 的注册、登录、五分钟再认证、恢复、撤销和审计；
2. 核对生产配置门禁、Nginx 双域名模板、OSS 媒体域名、后台文案和身份架构文档；
3. 生成不复用的生产密钥，写入服务器受限环境文件；
4. 安装 Node.js 24、Python 运行环境、Nginx 和 systemd 服务；
5. 连接 RDS，执行两次 `npm run db:migrate` 验证幂等，再运行 PostgreSQL 集成测试；
6. 导入 bootstrap 数据并验证公开读取；
7. 安装 web、acquisition worker 和 Frontier timer；
8. 配置 Nginx 双域名、TLS、安全头、限流和精确内部 API 放行；
9. 运行生产配置门禁、质量门禁、构建和 E2E；
10. 用真实 OSS 完成头像上传、320/800 WebP 访问、发布前 HEAD、替换、撤回、无引用对象清理、缓存头和费用告警演练；
11. 完成数据库恢复、回滚、密钥轮换、任务积压和告警演练。

### 阶段 C：负责人现场完成

1. 通过服务器本地命令生成一次性后台注册令牌；
2. 在 `https://admin.superones.top` 为 `lanzhouda@163.com` 注册 Passkey；
3. 建议至少注册两个独立认证器，例如电脑/密码管理器一个、手机或硬件密钥一个；
4. 下载或抄录恢复码，离线保存，不能放在服务器或 Git 仓库；
5. 验证登录、注销、高风险操作再认证和一次会话撤销；
6. 完成人工浏览器验收并作最终 Go/No-Go 决定。

## 5. 生产功能开关

部署阶段先使用以下安全状态；公开全功能首发前，支付和 Frontier 必须在真实验收后改为 `true`：

- `VAULT2077_OPC_PAYMENTS_ENABLED=false`：支付宝联调期间不收集订单联系人、不创建订单；真实小额支付、查询与退款全部通过后改为 `true`；
- `VAULT2077_FRONTIER_WRITES_ENABLED=false`：真实奖励未在后台发布并验收前，不开放报名、捐献和赛季写入；真实仓库闭环通过后改为 `true`；
- `VAULT2077_ALLOW_FILE_PREVIEW=false`：生产强制使用 PostgreSQL；
- `VAULT2077_RANGER_MEDIA_STORAGE=oss`：生产头像强制使用 OSS，并完整配置 `VAULT2077_OSS_*`；
- 两个 LLM 配置由负责人在服务器 Secret/环境文件中填写；
- OPC 服务、游骑兵和赛季奖励通过后台逐步发布，不因内容数量未填满阻塞网站上线。

## 6. 上线门禁

以下任何一项失败都不能正式开放相应写功能：

- `npm run deploy:check`、lint、typecheck、测试、构建和 E2E 未全部通过；
- 管理员原生 Passkey 未通过自动化测试，或尚未完成真实域名注册/登录/恢复演练；
- RDS 未使用 TLS 私网连接，或迁移、备份恢复未通过；
- OSS、`media.superones.top`、RAM 最小权限、头像撤回或无引用对象清理尚未完成真实联调；
- Node、数据库或内部管理接口可以绕过 Nginx 从公网访问；
- 告警主接收人已确定为 `lanzhouda@163.com`，当前无备用接收人；阿里云联系人组绑定和测试邮件仍需留下验收证据；
- Frontier 奖励未在后台正式发布，却对外显示为可报名赛季；
- 支付宝未完成真实小额订单和退款演练，却开启付款开关。

## 7. 当前责任划分

### 工程侧负责

- 原生 Passkey 管理员实现及测试；
- 生产配置、迁移、部署脚本、Nginx/systemd 校验；
- 数据导入、备份恢复验证、健康检查和技术告警；
- 自动测试、E2E、安全边界测试和最终技术报告；
- 支付宝凭证提供后完成接口联调和异常路径演练。

### 项目负责人必须完成

- 已开通的服务器、RDS 和头像 OSS 需确认受控的同地域私网链路；RDS 必须开启删除保护并完成真实 PITR 隔离恢复演练；
- 配置三个主机的 DNS/TLS、网络互通、OSS RAM 权限和控制台告警联系人；
- 提供服务器运维权限，但不把密码、Token 或私钥粘贴到聊天/Git；
- 在服务器环境中填写两个 LLM 凭证；
- 在真实域名完成 Passkey 注册，保存第二认证器和恢复码；
- 确定 RPO/RTO、最终发布人和安全/数据库告警接收人；
- 完成人工浏览器验收并签署 Go/No-Go；
- 上线后在后台逐季发布奖励、逐项发布 OPC、逐人发布游骑兵；
- 后续完成支付宝商家签约，将凭证安全注入后参与真实小额支付/退款验收；
- 对已形成的法律文本承担最终业务/法律确认责任。
